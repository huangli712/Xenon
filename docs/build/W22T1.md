# 实施方案: W22T1

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W22 — Tensor Construction |
| Task | W22T1 — Module skeleton: sub-module declarations only (pub use re-exports delegated to W22T2–W22T8) |
| 目标文件 | `src/construct/mod.rs, src/construct/init.rs` |
| 前置依赖 | None |
| 设计文档 | 18-construction.md |
| 预估工时 | 10 min |

## 目标

Module skeleton: sub-module declarations only（`pub use` 重导出由 W22T2–W22T8 各子任务负责）。本任务只完成 `src/construct/mod.rs, src/construct/init.rs` 中与该目标直接相关的实现计划，保持与 `18-construction.md` 的职责边界一致。

## 前置准备

1. 确认前置依赖 `None` 已完成并通过 `cargo check`。
2. 阅读 `18-construction §5, §6, §7, §8` 中的公共 API、内部实现、任务拆分和测试计划。
3. 打开 `src/construct/mod.rs, src/construct/init.rs`，确认只修改本任务目标范围，不重排相邻模块。

## 实施步骤

### Step 1: 定位实现边界

确认以下前置模块已可用（本 task 仅声明子模块，不引入实现依赖）：
- `crate::error::Result` — 后续构造函数将使用此错误类型
- `crate::dimension`、`crate::storage`、`crate::tensor` — 后续构造函数将依赖这些模块

本步骤只需确认 `cargo check` 通过，无需验证特定函数签名。

### Step 2: 实现任务目标

在 `src/construct/mod.rs, src/construct/init.rs` 中实现 `Module skeleton: sub-module declarations, public exports (declarations only, no impls)`，并保持错误返回、trait 约束和 public API 与 `18-construction §5, §6, §7, §8` 一致。

```rust
// Only module declarations — no re-exports, no impls.
// pub use re-exports are added organically by each sub-task:
//   W22T2 → pub use init::zeros;
//   W22T3 → pub use init::ones;
//   W22T4 → pub use eye::eye;
//   W22T5 → pub use from::{from_shape_vec, from_vec};
//   W22T6 → pub use from::from_shape_slice;
//   W22T7 → pub use from::from_array;
//   W22T8 → pub use scalar::from_scalar;
pub mod eye;
pub mod from;
pub mod init;
pub mod scalar;
```

### Step 3: 补齐导出与相邻调用点

确认 `src/construct/mod.rs` 中只有模块声明（`pub mod eye;` 等），不包含任何依赖后续 task 的 `pub use` 语句。如果 `src/lib.rs` 尚未注册 `pub mod construct;`，则同步注册。

### Step 4: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或者多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与Lint检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    /// Compile-time verification: all construction sub-modules are declared.
    /// Constructor behavior is tested in W22T2–W22T9.
    #[test]
    fn test_construct_module_declarations() {
        // Verify sub-module declarations compile — them existing is sufficient.
        // `super::*` imports all four declared modules: eye, from, init, scalar.
    }
}
```

## 关键设计决策

1. 依据 `18-construction §5, §6, §7, §8`，模块骨架一次性声明四个子模块，后续 task 在各自文件补实现。
2. `pub use` 重导出由各子任务自行添加到 `mod.rs`，不在本 task 中预先声明（避免引用尚未实现的函数）。
3. 依据 `18-construction.md` 的测试计划，本 task 的测试为编译期验证（模块声明可访问），构造函数正确性由 W22T2–W22T9 覆盖。
4. 依据 Wave 22 的依赖层级，本 task 不新增第三方依赖，也不改变前置模块的 public contract。

## 验证方式

1. 编译检查: `cargo check`
2. Lint 检查: `cargo clippy`
3. 测试验证: `cargo test test_construct_module_declarations`
4. 手动验证: 确认 `src/construct/mod.rs` 仅含 `pub mod` 声明，无 `pub use` 重导出。

## 注意事项

- 零长度轴是合法空张量，零维张量长度为 1。
- from_shape_vec 长度不匹配必须返回可恢复错误。
- eye 仅支持 Numeric，不把 bool 纳入单位矩阵 API。
