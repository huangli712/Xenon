# 实施方案: W11T2

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W11 — Broadcasting |
| Task | W11T2 — Module skeleton: file placeholder, rule function stubs |
| 目标文件 | `src/broadcast/shape.rs` |
| 前置依赖 | W11T1 |
| 设计文档 | 15-broadcast §5, §6, §7, §8 |
| 预估工时 | 10 min |

## 目标

Module skeleton: file placeholder, rule function stubs。

本任务在 `src/broadcast/shape.rs` 中完成对应实现，遵循 `SUMMARY.md` 对 W11T2 的粒度约束，并与 `15-broadcast.md` 中的公共 API、语义约束、测试计划保持一致。

## 前置准备

- W3/W6/W8 已完成：`Dimension`、`BroadcastDim`、layout flags、`TensorBase` 与只读 view 构造入口可用。
- W2 已完成：`XenonError::BroadcastError` 与参数错误可用于结构化失败返回。

## 实施步骤

### Step 1: 定义 shape API 骨架

按照 `15-broadcast.md` 的接口边界实现本步骤，保持单文件内最小修改。

```rust
use crate::dimension::IxDyn;
use crate::error::XenonError;

pub fn can_broadcast(shape_a: &[usize], shape_b: &[usize]) -> bool {
    broadcast_shape(shape_a, shape_b).is_ok()
}

pub fn broadcast_shape(shape_a: &[usize], shape_b: &[usize]) -> Result<IxDyn, XenonError> {
    let _ = (shape_a, shape_b);
    todo!("implemented by W11T5")
}

pub fn broadcast_strides(
    orig_shape: &[usize],
    orig_strides: &[usize],
    target_shape: &[usize],
) -> Result<Vec<usize>, XenonError> {
    let _ = (orig_shape, orig_strides, target_shape);
    todo!("implemented by W11T6")
}
```
### Step 2: 保留结构化错误入口

按照 `15-broadcast.md` 的接口边界实现本步骤，保持单文件内最小修改。

```rust
fn broadcast_error(axis: usize, left: usize, right: usize) -> XenonError {
    XenonError::BroadcastError {
        axis,
        left,
        right,
    }
}
```
### Step 3: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或者多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与Lint检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_shape_stub_signatures_compile() {
        let _: fn(&[usize], &[usize]) -> bool = can_broadcast;
        let _: fn(&[usize], &[usize]) -> Result<IxDyn, XenonError> = broadcast_shape;
        let _: fn(&[usize], &[usize], &[usize]) -> Result<Vec<usize>, XenonError> = broadcast_strides;
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| 广播按尾轴右对齐逐轴判定 | 15-broadcast §1.2 和 §5.2 要求与 Numpy 规则一致 |
| 广播视图始终只读且零拷贝 | 15-broadcast §1.1、§6.1 明确禁止可写广播视图 |
| 非空零步长视图重算为 `BroadcastView` | 15-broadcast §8.2 和 §9.1 要求 flags 与零步长语义一致 |

## 验证方式

1. 编译检查: `cargo check`
2. Lint 检查: `cargo clippy --all-targets`
3. 测试验证: `cargo test broadcast`
4. 手动验证: 编写一个 `[1, 3] -> [2, 3]` 的广播 view，确认 shape 为 `[2, 3]`、广播轴 stride 为 `0`、data pointer 与源张量一致。

## 注意事项

- 空轴广播允许存在，但空数组退化零步长不应错误标记为非空 `BroadcastView`。
- `broadcast_to()` 不得复制底层存储，也不得返回任何可写 view。
- `broadcast_with()` 的类型 bound 必须与 15-broadcast §5.1 的双向 `BroadcastDim` 约束保持一致。
