# 实施方案: W20T2

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W20 — Shape Operations |
| Task | W20T2 — Module skeleton: transpose.rs declarations |
| 目标文件 | `src/shape/transpose.rs` |
| 前置依赖 | W20T1 |
| 设计文档 | 16-shape §5, §6, §7, §8 |
| 预估工时 | 10 min |

## 目标

Create `transpose.rs` with the method signature, required trait bounds, and metadata-only construction path.

## 前置准备

- 阅读 `16-shape` 对应的公共 API、实现拆分和测试计划。
- 确认前置任务 `W20T1` 已完成；若为 `None`，确认基础 tensor、dimension、storage、error 模块已按前序 wave 提供。
- 在修改前运行 `cargo check` 获取当前基线，避免把既有失败误认为本任务引入。

## 实施步骤

### Step 1: Define method skeleton

实现该步骤时只修改任务范围内的目标文件，保持与现有模块风格一致。

```rust
use crate::dimension::{Dimension, Reverse};
use crate::layout::{compute_layout_flags, Strides};
use crate::storage::{Storage, ViewRepr};
use crate::tensor::{TensorBase, TensorView};

impl<S, D, A> TensorBase<S, D>
where
    S: Storage<Elem = A>,
    D: Dimension + Reverse,
{
    pub fn transpose(&self) -> TensorView<'_, A, D> {
        transpose_impl(self)
    }
}
```
### Step 2: Define helper signature

实现该步骤时只修改任务范围内的目标文件，保持与现有模块风格一致。

```rust
fn transpose_impl<S, D, A>(tensor: &TensorBase<S, D>) -> TensorView<'_, A, D>
where
    S: Storage<Elem = A>,
    D: Dimension + Reverse,
{
    let new_shape = tensor.raw_dim().clone().reverse();
    let new_strides: Strides<D> = tensor.strides().clone().reverse();
    let new_flags = compute_layout_flags::<A, D>(&new_shape, &new_strides, tensor.as_ptr());
    TensorView::from_raw_view(ViewRepr::from_storage(tensor.storage()), new_shape, new_strides, tensor.offset(), new_flags)
}
```
### Step 3: 单元测试

按设计文档测试计划添加或更新单元测试，覆盖正常路径、边界情况和错误路径。

```rust
#[cfg(test)]
mod tests {
    use crate::{Ix1, Tensor1};

    #[test]
    fn test_transpose_1d_noop() {
        let x = Tensor1::from_shape_vec(Ix1(3), vec![1_i32, 2, 3]).unwrap();
        let y = x.transpose();
        assert_eq!(y.shape(), &[3]);
        assert_eq!(y.len(), x.len());
    }
}
```
## 关键设计决策

- `Reverse` is named in the public where-clause and remains sealed by the dimension module.
- The helper returns `TensorView` so transpose is zero-copy.
- Flags are recomputed rather than copied from the source tensor.

## 验证方式

1. 执行 ``cargo check``。
2. 执行 ``cargo test shape::transpose::tests::test_transpose_1d_noop``。
3. 手动验证: 打开 `src/shape/transpose.rs`，确认实现只覆盖 W20T2 范围，并与 16-shape §5, §6, §7, §8 的 API、错误语义和测试计划一致。

## 注意事项

- Use existing unchecked view constructor names if they differ from this illustrative `from_raw_view`.
- 所有代码示例依赖前序 wave 已定义的 crate 类型和 trait；若实际名称不同，按现有代码中的同义 API 做最小替换。
- 不添加超出设计文档范围的新公开 API、兼容层或占位功能。
