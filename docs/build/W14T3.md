# 实施方案: W14T3

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W14 — SIMD Backend |
| Task | W14T3 — Float sum SIMD: f32/f64 SumKernel with lane accumulation + documented merge |
| 目标文件 | `src/simd/vector.rs` |
| 前置依赖 | W14T2 |
| 设计文档 | 08-simd.md |
| 预估工时 | 10 min |

## 目标

Float sum SIMD: f32/f64 SumKernel with lane accumulation + documented merge。

本任务在 `src/simd/vector.rs` 中完成对应实现切片，严格遵守 08-simd §5, §6, §7, §8 的范围边界：先保证标量语义、错误边界与 feature gate 正确，再接入可选加速或模块导出。示例代码按当前任务展示核心实现形态，依赖 SUMMARY 中已完成的前置任务提供的 Tensor、Dimension、Element、Error、Dispatch 等类型。

## 前置准备

- 确认前置任务已完成：W14T2。
- 阅读 `src/simd/vector.rs` 所属模块现有代码，避免重写相邻任务已实现的逻辑。
- 对照 08-simd §5, §6, §7, §8 的 API、内部实现和测试计划，确认本任务只修改目标文件列出的范围。
- 若涉及 feature gate，分别准备默认特性和相关特性启用后的验证命令。

## 实施步骤

### Step 1: Add `SumF32Kernel` and `SumF64Kernel` using lane-local accumulation

Add `SumF32Kernel` and `SumF64Kernel` using lane-local accumulation.

Use `get_arch()` from `mod.rs` (W14T1). Define `SumF32Kernel` / `SumF64Kernel` with lane-local accumulation via `pulp::WithSimd`. Horizontally merge lanes with documented tolerance semantics. Handle scalar tail after vector body.

### Step 2: Horizontally merge lanes with documented tolerance semantics

Horizontally merge lanes with documented tolerance semantics.

### Step 3: Handle scalar tail after the vector body

Handle scalar tail after the vector body.

### Step 4: Expose `try_sum_f32` and `try_sum_f64` facades that apply the 1024 element threshold

Expose `try_sum_f32` and `try_sum_f64` facades that apply the 1024 element threshold.

### Step 5: 单元测试

添加覆盖设计文档测试计划的单元测试，至少包含正常路径和一个边界或回退场景。

```rust
#[cfg(all(test, feature = "simd"))]
mod tests {
    use super::*;

    #[test]
    fn test_simd_sum_f32_matches_serial_with_tolerance() {
        let data: Vec<f32> = (0..128).map(|v| v as f32).collect();
        let simd_result = try_sum_f32(&data);
        if let Some(simd) = simd_result {
            let scalar: f32 = data.iter().sum();
            // Floating-point sum may differ by documented tolerance
            let relative_diff = (simd - scalar).abs() / scalar.abs().max(1e-6);
            assert!(relative_diff < 1e-5,
                "SIMD sum {simd} deviates too much from scalar {scalar}");
        }
    }

    #[test]
    fn test_simd_sum_short_input_returns_none() {
        let data: Vec<f32> = (0..64).map(|v| v as f32).collect();
        assert!(try_sum_f32(&data).is_none(),
            "Short input should fall back to scalar");
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| 只实现本 task 对应切片 | SUMMARY.md 将 W14-W16 拆成单文件粒度任务，避免跨 task 重构 |
| Float reductions may use documented tolerance after horizontal merge; they must not promise bitwise equality across SIMD and scalar paths. | 08-simd §5, §6, §7, §8 |
| 单元测试与实现同任务提交 | TEMPLATE.md 要求最后一个实施步骤必须包含单元测试 |

## 验证方式

1. 编译检查: `cargo check`
2. Feature 编译: 根据任务范围运行 `cargo check --features simd`、`cargo check --features parallel` 或两者组合
3. 测试验证: `cargo test --features simd test_simd_sum_f64_matches_serial_with_tolerance`
4. 回归验证: `cargo test`，涉及可选后端时再运行对应 feature 的测试命令
5. 手动验证: 阅读生成的 rustdoc 或测试输出，确认公开/内部 API 路径与预期模块路径一致

## 注意事项

- SIMD module does not own scalar fallback loops; callers keep the serial baseline.
- All SIMD entries are internal `pub(crate)` APIs and must stay feature-gated.
- Unsafe code requires explicit length, tail, and alignment proof before vector load/store.
- 所有代码注释和 Rust 标识符使用英文，文档说明保持当前实施方案风格。
- 不添加新的第三方依赖，除非设计文档已在 Cargo feature 中声明。
