# 实施方案: W8T8

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W8 — Tensor Core |
| Task | W8T8 — from_raw_vec_unchecked (pub(crate) unsafe internal constructor) |
| 目标文件 | `src/tensor/construct.rs` |
| 前置依赖 | W8T5, W8T7, W6T5 |
| 设计文档 | 07-tensor.md |
| 预估工时 | 15 min |

## 目标

from_raw_vec_unchecked (pub(crate) unsafe internal constructor)。
本任务将 SUMMARY.md 中的目标落实到 `src/tensor/construct.rs`，并与 `07-tensor §5, §6, §7, §8` 的 API、实现拆分和测试计划保持一致。

## 前置准备

- 确认 W6T5 已提供 layout flags 与 F-order stride 计算。
- 确认 W7 存储模块已提供 Owned/View/ViewMut/Arc 表示和 storage trait。
- 按 `07-tensor.md §5` 保持 tensor core 的最小职责。

## 实施步骤

### Step 1: 实现核心代码

在 `src/tensor/construct.rs` 中添加本任务要求的最小实现，保持职责边界与设计文档一致。

```rust
impl<A, D> TensorBase<crate::storage::Owned<A>, D>
where
    A: crate::element::Element,
    D: crate::dimension::Dimension,
{
    pub(crate) unsafe fn from_raw_vec_unchecked(data: Vec<A>, shape: D) -> Self {
        let strides = crate::layout::compute_f_strides(&shape);
        let storage = crate::storage::Owned::from_vec(data);
        let flags = crate::layout::compute_layout_flags::<A, D>(&shape, &strides, storage.as_ptr());
        unsafe { Self::new_unchecked(storage, shape, strides, 0, flags, false) }
    }
}
```

### Step 2: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或者多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与Lint检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

测试依据: `test_from_raw_vec_unchecked_valid` + `test_from_raw_vec_unchecked_empty` + `test_from_raw_vec_unchecked_zero_dim` 覆盖正常/空数据/零维度边界。

```rust
#[cfg(test)]
mod tests {
    use crate::dimension::Ix0;
    use crate::tensor::TensorBase;

    #[test]
    fn test_from_raw_vec_unchecked_valid() {
        let tensor = unsafe {
            TensorBase::from_raw_vec_unchecked(
                vec![1_i32, 2, 3, 4],
                [2, 2],
            )
        };
        assert_eq!(tensor.len(), 4);
        assert!(tensor.is_f_contiguous());
    }

    #[test]
    fn test_from_raw_vec_unchecked_empty() {
        let tensor = unsafe {
            TensorBase::from_raw_vec_unchecked(
                Vec::<i32>::new(),
                [0, 3],
            )
        };
        assert_eq!(tensor.len(), 0);
        assert!(tensor.is_f_contiguous());
    }

    #[test]
    fn test_from_raw_vec_unchecked_zero_dim() {
        // Ix0 shape: scalar-like, expects exactly 1 element
        let tensor = unsafe {
            TensorBase::from_raw_vec_unchecked(
                vec![42_i32],
                Ix0(),
            )
        };
        assert_eq!(tensor.ndim(), 0);
        assert_eq!(tensor.len(), 1);
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| `from_raw_vec_unchecked` 标记 `pub(crate) unsafe` | `07-tensor.md §5.6`：仅 crate 内部可用；外部通过 W22 公共构造器（`from_shape_vec`）间接使用 |
| 内部自动计算 F-strides + LayoutFlags | 减少调用方认知负担，同时保证内部构造一致性 |
| 构造函数完成即可直接用于 W8T4/W8T5 测试 | 提供自包含的 tensor 构造路径，解耦 W22 依赖 |

## 注意事项

- 调用方必须保证 `data.len() == shape.checked_size()`，否则后续访问越界。
- 零维度 shape（Ix0）传入 `vec![x]` 是合法的（1 个元素），传入空 vec 导致 len=0 但 shape 不匹配。
- 此构造器是 W22 公共构造器的内部基础，W22 会在外围做 shape 验证后调用此函数。
