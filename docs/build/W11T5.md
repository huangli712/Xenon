# 实施方案: W11T5

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W11 — Broadcasting |
| Task | W11T5 — broadcast_shape(): shared shape derivation with structured broadcast errors |
| 目标文件 | `src/broadcast/shape.rs` |
| 前置依赖 | W11T4（`can_broadcast` 已落地）、W3T10（`IxDyn::from_vec`） |
| 设计文档 | 15-broadcast.md §5.1（签名）、§5.2 line 167（语义）、§6.2 line 220-226（算法伪码）；26-error.md §5.1 line 124-130（BroadcastError 字段） |
| 预估工时 | 10 min |

## 目标

替换 W11T2 留下的 `broadcast_shape` `todo!()` 骨架，实现"右对齐逐轴推导广播 shape，不兼容时返回结构化 `BroadcastError`"。同时把 `can_broadcast` 重写为 `broadcast_shape(...).is_ok()`（语义等价，避免逻辑重复）。

## 前置准备

- W11T2 已完成：`broadcast_error` helper（5 字段对齐 26-error §5.1）就位。
- W11T4 已完成：`can_broadcast` 独立实现可用；本 task 将把它重写为复用 `broadcast_shape`。
- W3T10 已完成：`IxDyn::from_vec(Vec<usize>) -> Self` 关联函数可用（参见 02-dimension §5.5 line 425）。
- **不依赖** `From<Vec<usize>>` —— 02-dimension §5.5 未为 `IxDyn` 实现 `From<Vec<usize>>`。

## 实施步骤

### Step 1: 实现 `broadcast_shape`（严格对齐 §6.2 伪码）

按 `15-broadcast.md §6.2` line 220-226 五步算法：(1) 右对齐；(2) 缺失前导轴按 1；(3) 不等且都不为 1 → BroadcastError；(4) 取较大值（含 `1 -> 0` 空轴退化）；(5) 返回 IxDyn：

```rust
/// Compute the broadcast result shape of two input shapes. See
/// `15-broadcast.md §5.2` line 167 / §6.2 line 220-226.
///
/// Algorithm (right-align):
///   1. Align dimensions from right to left.
///   2. Missing leading dimensions are treated as 1.
///   3. If two aligned dimensions differ and neither is 1, return BroadcastError.
///   4. Otherwise result = max(a, b): if one is 1, take the other; if both equal
///      (including both 1 or both 0 for empty-axis broadcast), take that value.
///   5. Return the resulting IxDyn shape.
pub fn broadcast_shape(shape_a: &[usize], shape_b: &[usize]) -> Result<IxDyn, XenonError> {
    let ndim = shape_a.len().max(shape_b.len());
    let mut out = vec![1usize; ndim];

    // Iterate over the result axes from index 0 (leading) to ndim-1 (trailing),
    // right-aligning the input shapes per §6.2 step 1.
    for out_axis in 0..ndim {
        let from_back = ndim - 1 - out_axis;
        // §6.2 step 2: missing leading axes are length 1.
        let a = shape_a
            .len()
            .checked_sub(from_back + 1)
            .map(|i| shape_a[i])
            .unwrap_or(1);
        let b = shape_b
            .len()
            .checked_sub(from_back + 1)
            .map(|i| shape_b[i])
            .unwrap_or(1);

        out[out_axis] = match (a, b) {
            (x, y) if x == y => x,    // §6.2 step 4: equal (covers `0 == 0` empty axis).
            (1, y) => y,              // §6.2 step 4: a is 1 → take b.
            (x, 1) => x,              // §6.2 step 4: b is 1 → take a.
            _ => {
                // §6.2 step 3 + 26-error §5.1: structured error with all 5 fields.
                // No `attempted_target_shape` — this is the pure shape-derivation path
                // (single target is meaningful only in `broadcast_to`).
                return Err(broadcast_error(
                    "broadcast_shape",
                    shape_a,
                    shape_b,
                    None,
                    out_axis,
                ));
            }
        };
    }

    // 02-dimension §5.5 line 425: `IxDyn::from_vec(Vec<usize>) -> Self`.
    // `IxDyn` does NOT impl `From<Vec<usize>>`, must use `from_vec`.
    Ok(IxDyn::from_vec(out))
}
```

### Step 2: 把 `can_broadcast` 重写为复用 `broadcast_shape`

§5.2 line 166 与 line 167 隐含 `can_broadcast(a, b) == broadcast_shape(a, b).is_ok()`（§8.4 属性测试不变量第 1 行明确）。重写后逻辑单一来源、易维护：

```rust
/// Numpy-style broadcast compatibility check. Semantically equivalent to
/// `broadcast_shape(a, b).is_ok()` per `15-broadcast.md §8.4` invariant.
pub fn can_broadcast(shape_a: &[usize], shape_b: &[usize]) -> bool {
    broadcast_shape(shape_a, shape_b).is_ok()
}
```

### Step 3: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或者多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与Lint检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

覆盖 §8.2 中 `test_broadcast_shape_basic` / `test_broadcast_shape_error` 两项，并附加 §8.3 边界（空轴 / 标量到高维）以及一项 §8.4 属性不变量校验：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::error::XenonError;

    #[test]
    fn test_broadcast_shape_basic() {
        // Right-align with `1` axis expansion.
        let r = broadcast_shape(&[1, 3], &[2, 3]).unwrap();
        assert_eq!(r.slice(), &[2, 3]);
        // Scalar to high-dim: missing leading axes are 1.
        let r = broadcast_shape(&[], &[2, 3]).unwrap();
        assert_eq!(r.slice(), &[2, 3]);
        // Empty-axis broadcast `[0, 3]` vs `[1, 3]` → `[0, 3]` (§8.3 row 1).
        let r = broadcast_shape(&[0, 3], &[1, 3]).unwrap();
        assert_eq!(r.slice(), &[0, 3]);
        // High-rank cross-broadcast `[2,1,4]` → `[3,2,5,4]` (§8.3 row 4).
        let r = broadcast_shape(&[2, 1, 4], &[3, 2, 5, 4]).unwrap();
        assert_eq!(r.slice(), &[3, 2, 5, 4]);
    }

    #[test]
    fn test_broadcast_shape_error() {
        let err = broadcast_shape(&[2, 3], &[4, 3]).unwrap_err();
        match err {
            XenonError::BroadcastError {
                operation,
                lhs_shape,
                rhs_shape,
                attempted_target_shape,
                axis,
            } => {
                assert_eq!(operation.as_ref(), "broadcast_shape");
                assert_eq!(lhs_shape, vec![2, 3]);
                assert_eq!(rhs_shape, vec![4, 3]);
                assert_eq!(attempted_target_shape, None);
                // Mismatch at result axis 0 (leading).
                assert_eq!(axis, Some(0));
            }
            other => panic!("expected BroadcastError, got {:?}", other),
        }
    }

    /// §8.4 invariant: `can_broadcast(a, b) == broadcast_shape(a, b).is_ok()`.
    #[test]
    fn test_can_broadcast_matches_broadcast_shape() {
        let cases = [
            (&[1, 3][..], &[2, 3][..]),
            (&[2, 3][..], &[4, 3][..]),
            (&[][..], &[2, 3][..]),
            (&[0, 3][..], &[1, 3][..]),
            (&[2, 1, 4][..], &[3, 2, 5, 4][..]),
        ];
        for (a, b) in cases {
            assert_eq!(can_broadcast(a, b), broadcast_shape(a, b).is_ok(),
                "mismatch on {:?} vs {:?}", a, b);
        }
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| 算法严格对齐 §6.2 五步伪码 | 15-broadcast §6.2 line 220-226 |
| `attempted_target_shape` 在 `broadcast_shape` 路径中填 `None` | 该字段语义为"caller-supplied target"，纯双输入 shape 推导路径不存在显式 target；`broadcast_to` 路径才在 W11T6/T8 中填入 |
| 使用 `IxDyn::from_vec` 而非 `IxDyn::from` | 02-dimension §5.5 line 419-426 仅定义 `from_vec` / `from_slice` / `zeros` / `into_vec`；`IxDyn` 无 `From<Vec<usize>>` impl |
| `can_broadcast` 重写为 `broadcast_shape(...).is_ok()` | 15-broadcast §8.4 line 393 不变量；单一来源避免双份维护 |

## 验证方式

1. 编译检查: `cargo check`
2. Lint 检查: `cargo clippy --all-targets`
3. 测试验证: `cargo test --lib broadcast::shape::tests::test_broadcast_shape_basic broadcast::shape::tests::test_broadcast_shape_error broadcast::shape::tests::test_can_broadcast_matches_broadcast_shape`
4. 手动验证: 在 REPL 中调用 `xenon::broadcast::broadcast_shape(&[2, 1, 4], &[3, 2, 5, 4])` 应返回 `Ok(IxDyn([3, 2, 5, 4]))`。

## 注意事项

- 严禁出现 `IxDyn::from(...)` —— 02-dimension §5.5 中 `IxDyn` 未实现 `From<Vec<usize>>`。
- `broadcast_error` 的 `axis` 参数填**结果轴下标**（从 0 起算的 leading 轴），不是 `from_back`。
- 本 task 完成后，W11T4 的独立 `can_broadcast` 实现被本 task 重写为复用形式 —— W11T4 的测试仍应通过（行为等价）。
- 不要在本 task 修改 `broadcast_strides` 或引入 `view.rs` 相关项。
