# 实施方案: W6T4

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W6 — Layout System |
| Task | W6T4 — Module skeleton: file placeholder, declarations |
| 目标文件 | `src/layout/contiguous.rs` |
| 前置依赖 | W6T1 |
| 设计文档 | 06-layout.md |
| 预估工时 | 5 min |

## 目标

Module skeleton: file placeholder, declarations

按照 `build/SUMMARY.md` 中 W6T4 的定义，在 `src/layout/contiguous.rs` 中完成此 task 的最小实现，并保持与 `docs/design/06-layout.md` §5（公共 API）、§6（实现细节）、§7（错误/安全约束）和 §8（测试计划）一致。

## 前置准备

- 完成前置任务：W6T1
- 确认 `dimension` 与 `error` 模块已提供设计中引用的基础 API

## 实施步骤

### Step 1: Create contiguous module placeholder

Create `src/layout/contiguous.rs` with a minimal compiling function signature. W6T7 will replace the body with the full recognition algorithm.

```rust
use crate::dimension::Dimension;
use super::Strides;

pub fn is_f_contiguous<D: Dimension>(_shape: &D, _strides: &Strides<D>) -> bool {
    true
}
```
### Step 2: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或者多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与Lint检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

参考 `docs/design/06-layout.md` 的「测试计划」为本 task 增加以下测试：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_f_contig_empty() {
        let shape = [0_usize, 3];
        let strides = Strides::new([1_usize, 0]);
        assert!(is_f_contiguous(&shape, &strides));
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| `is_f_contiguous` 骨架返回 `true`（占位） | `06-layout §5.7` 的空数组/标量快速路径总是连续的；真正的识别算法在 W6T7 中实现 |

## 验证方式

1. 编译检查: `cargo check -p xenon`
2. Lint 检查: `cargo clippy --all-targets`
3. 测试验证: `cargo test layout::contiguous::tests::test_f_contig_empty`
4. 手动验证: 占位函数对任意输入始终返回 `true`；W6T7 将替换为实际检测逻辑。

## 注意事项

- 本 task 为 **占位符**：真正的 F-contiguity 检测算法在 W6T7 中实现。
- 骨架函数签名必须与设计文档 §5.7 一致，确保 W6T7 可无缝替换。
- 当前函数忽略所有输入参数——W6T7 将使用 shape + strides 进行反向遍历检测。
