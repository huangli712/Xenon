# 实施方案: W15T5

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W15 — Parallel Backend |
| Task | W15T5 — par_dot: ndim==1 check, length consistency, parallel inner product, error return + empty identity |
| 目标文件 | `src/parallel/reduce.rs` |
| 前置依赖 | W15T4 |
| 设计文档 | 09-parallel.md |
| 预估工时 | 10 min |

## 目标

par_dot: ndim==1 check, length consistency, parallel inner product, error return + empty identity。

本任务在 `src/parallel/reduce.rs` 中完成对应实现切片，严格遵守 09-parallel §5, §6, §7, §8 的范围边界：先保证标量语义、错误边界与 feature gate 正确，再接入可选加速或模块导出。示例代码按当前任务展示核心实现形态，依赖 SUMMARY 中已完成的前置任务提供的 Tensor、Dimension、Element、Error、Dispatch 等类型。

## 前置准备

- 确认前置任务已完成：W15T4。
- 阅读 `src/parallel/reduce.rs` 所属模块现有代码，避免重写相邻任务已实现的逻辑。
- 对照 09-parallel §5, §6, §7, §8 的 API、内部实现和测试计划，确认本任务只修改目标文件列出的范围。
- 若涉及 feature gate，分别准备默认特性和相关特性启用后的验证命令。

## 实施步骤

### Step 1: Validate both operands are one-dimensional

Validate both operands are one-dimensional.

```rust
#[cfg(feature = "parallel")]
pub(crate) fn par_dot<SL, SR, A, DL, DR>(
    lhs: &TensorBase<SL, DL>,
    rhs: &TensorBase<SR, DR>,
    strategy: &ParallelExecStrategy,
    _guard: ParallelGuard,
) -> Result<A, XenonError>
where
    SL: Storage<Elem = A>,
    SR: Storage<Elem = A>,
    DL: Dimension,
    DR: Dimension,
    A: Numeric + Send + Sync;
```

### Step 2: Validate equal lengths before scheduling work

Validate equal lengths before scheduling work.

### Step 3: Compute chunk-local products and reduce with the additive identity

Compute chunk-local products and reduce with the additive identity.

### Step 4: Return shape errors for rank or length mismatch

Return shape errors for rank or length mismatch.

### Step 5: 单元测试

添加覆盖设计文档测试计划的单元测试，至少包含正常路径和一个边界或回退场景。

```rust
#[cfg(all(test, feature = "parallel"))]
mod tests {
    use super::*;

    #[test]
    fn test_par_dot_shape_mismatch() {
        let a = Tensor::from_vec(vec![1.0f64, 2.0, 3.0], Ix1(3)).unwrap();
        let b = Tensor::from_vec(vec![1.0f64, 2.0], Ix1(2)).unwrap();
        let strategy = ParallelExecStrategy::default();
        let guard = ParallelGuard::new_for_test();
        let result = par_dot(&a, &b, &strategy, guard);
        assert!(result.is_err());
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| 只实现本 task 对应切片 | SUMMARY.md 将 W14-W16 拆成单文件粒度任务，避免跨 task 重构 |
| Rank and length checks happen before scheduling work to keep error behavior deterministic. | 09-parallel §5, §6, §7, §8 |
| 单元测试与实现同任务提交 | TEMPLATE.md 要求最后一个实施步骤必须包含单元测试 |

## 验证方式

1. 编译检查: `cargo check`
2. Feature 编译: 根据任务范围运行 `cargo check --features simd`、`cargo check --features parallel` 或两者组合
3. 测试验证: `cargo test --features parallel test_par_dot_shape_mismatch`
4. 回归验证: `cargo test`，涉及可选后端时再运行对应 feature 的测试命令
5. 手动验证: 阅读生成的 rustdoc 或测试输出，确认公开/内部 API 路径与预期模块路径一致

## 注意事项

- Parallel functions assume dispatch has already selected the parallel path.
- Outer `ParallelGuard` must stay on the dispatching thread and must not be captured by workers.
- Feature-gated tests must prove default builds work without Rayon.
- 所有代码注释和 Rust 标识符使用英文，文档说明保持当前实施方案风格。
- 不添加新的第三方依赖，除非设计文档已在 Cargo feature 中声明。
- 本文件 `src/parallel/reduce.rs` 由 W15T4 (par_reduce_impl + par_sum) 和 W15T5 (par_dot) 共享。W15T4 已创建文件骨架和 par_reduce_impl/par_sum。本 task 增量添加 par_dot 函数，注意不覆盖 W15T4 已有代码。
