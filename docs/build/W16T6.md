# 实施方案: W16T6

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W16 — Math Operations |
| Task | W16T6 — Arithmetic ops: add, sub, mul, div scalar variants via shared binary skeleton |
| 目标文件 | `src/math/binary.rs` |
| 前置依赖 | W16T2 |
| 设计文档 | 11-math.md |
| 预估工时 | 10 min |

## 目标

Arithmetic ops: add, sub, mul, div scalar variants via shared binary skeleton。

本任务在 `src/math/binary.rs` 中完成对应实现切片，严格遵守 11-math §5, §6, §7, §8 的范围边界：先保证标量语义、错误边界与 feature gate 正确，再接入可选加速或模块导出。示例代码按当前任务展示核心实现形态，依赖 SUMMARY 中已完成的前置任务提供的 Tensor、Dimension、Element、Error、Dispatch 等类型。

## 前置准备

- 确认前置任务已完成：W16T2。
- 阅读 `src/math/binary.rs` 所属模块现有代码，避免重写相邻任务已实现的逻辑。
- 对照 11-math §5, §6, §7, §8 的 API、内部实现和测试计划，确认本任务只修改目标文件列出的范围。
- 若涉及 feature gate，分别准备默认特性和相关特性启用后的验证命令。

## 实施步骤

### Step 1: Expose tensor-tensor add, sub, mul, and div methods

Expose tensor-tensor add, sub, mul, and div methods.

```rust
// Tensor-tensor arithmetic, routed through the broadcast-aware
// apply_binary skeleton from W16T2.  Checked arithmetic and
// division-by-zero panic semantics per 11-math §5.3.

impl<S, D, A> TensorBase<S, D>
where
    S: Storage<Elem = A>,
    D: Dimension,
    A: Numeric,
{
    pub fn add<S2, E>(&self, other: &TensorBase<S2, E>)
        -> Result<Tensor<A, <D as BroadcastDim<E>>::Output>, XenonError>
    where
        S2: Storage<Elem = A>,
        D: BroadcastDim<E>,
        E: Dimension,
    {
        apply_binary(self, other, |a, b| add_or_panic(a, b))
    }
    // sub, mul, div follow the same shape.
}
```

### Step 2: Expose scalar variants for the same arithmetic operations

Expose scalar variants for the same arithmetic operations. Each tensor-scalar
variant wraps a broadcast of the scalar into a same-shape tensor and delegates
to the tensor-tensor helper.

```rust
impl<S, D, A> TensorBase<S, D>
where
    S: Storage<Elem = A>,
    D: Dimension,
    A: Numeric,
{
    pub fn add_scalar(&self, scalar: A) -> Tensor<A, D> {
        let other = Tensor::from_scalar(scalar);
        self.add(&other).unwrap()
    }
    // sub_scalar, mul_scalar, div_scalar follow the same shape.
}
```

### Step 3: Route tensor-tensor operations through the broadcast-aware helper

Route tensor-tensor operations through the broadcast-aware helper.

### Step 4: Use checked integer arithmetic and division-by-zero panic semantics

Use checked integer arithmetic and division-by-zero panic semantics.

### Step 5: 单元测试

添加覆盖设计文档测试计划的单元测试，至少包含正常路径和一个边界或回退场景。

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::Tensor;

    #[test]
    fn test_add_broadcast() {
        // [3,1] + [1,4] broadcast → [3,4].
        let a = Tensor::<f64, Ix2>::from_shape_vec(
            [3, 1], vec![1.0, 2.0, 3.0],
        ).unwrap();
        let b = Tensor::<f64, Ix2>::from_shape_vec(
            [1, 4], vec![10.0, 20.0, 30.0, 40.0],
        ).unwrap();
        let c = a.add(&b).unwrap();
        assert_eq!(c.shape(), &[3, 4]);
        assert!((c[[0, 0]] - 11.0).abs() < 1e-10);
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| 只实现本 task 对应切片 | SUMMARY.md 将 W14-W16 拆成单文件粒度任务，避免跨 task 重构 |
| Scalar variants reuse the binary execution semantics without adding a separate broadcasting model. | 11-math §5, §6, §7, §8 |
| 单元测试与实现同任务提交 | TEMPLATE.md 要求最后一个实施步骤必须包含单元测试 |

## 验证方式

1. 编译检查: `cargo check`
2. Feature 编译: 根据任务范围运行 `cargo check --features simd`、`cargo check --features parallel` 或两者组合
3. 测试验证: `cargo test test_add_broadcast test_mul_scalar`
4. 回归验证: `cargo test`，涉及可选后端时再运行对应 feature 的测试命令
5. 手动验证: 阅读生成的 rustdoc 或测试输出，确认公开/内部 API 路径与预期模块路径一致

## 注意事项

- Math methods define user-visible semantics; acceleration backends cannot change shape, errors, NaN behavior, or panic boundaries.
- Broadcast incompatibility is recoverable, while integer overflow and division by zero are panic paths.
- Do not expose generic map helpers as public API in this wave.
- 所有代码注释和 Rust 标识符使用英文，文档说明保持当前实施方案风格。
- 不添加新的第三方依赖，除非设计文档已在 Cargo feature 中声明。
