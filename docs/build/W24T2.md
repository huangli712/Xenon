# 实施方案: W24T2

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W24 — Utility Operations |
| Task | W24T2 — fill(): StorageMut-level fill helper + try_fill() dispatch for all tensor types |
| 目标文件 | `src/util/fill.rs` |
| 前置依赖 | W24T1, W7, W8, W12, W2 |
| 设计文档 | 20-utility.md |
| 预估工时 | 10 min |

## 目标

fill(): StorageMut-level fill helper + try_fill() dispatch for all tensor types

按照 `build/SUMMARY.md` 中 W24T2 的定义，在 `src/util/fill.rs` 中完成此 task 的最小实现，并保持与 `docs/design/20-utility.md` §5（公共 API）、§6（实现细节）、§7（任务拆分/约束）和 §8（测试计划）一致。

## 前置准备

- 完成 W24T1 模块骨架。
- 确认 `StorageMut`、`TensorBase`、迭代器和 `XenonError::InvalidStorageMode` 可用。

## 实施步骤

### Step 1: Implement stride-aware fill for writable storage

Add the primary `fill()` inherent method for `S: StorageMut`. It must write only logical elements through the mutable iterator path and must not touch padding bytes.

```rust
impl<S, D, A> TensorBase<S, D>
where
    S: StorageMut<Elem = A>,
    D: Dimension,
    A: Element + Clone,
{
    pub fn fill(&mut self, value: A) {
        for slot in self.iter_mut() {
            *slot = value.clone();
        }
    }
}
```

### Step 2: Implement fallible try_fill dispatch

Add `try_fill()` on readable tensors and route to the storage layer's mutable capability helper. Read-only and shared-read-only storage must return `InvalidStorageMode` instead of silently doing nothing.

```rust
impl<S, D, A> TensorBase<S, D>
where
    S: Storage<Elem = A>,
    D: Dimension,
    A: Element + Clone,
{
    pub fn try_fill(&mut self, value: A) -> Result<(), XenonError> {
        let storage = self.storage_mut_or_err()?;
        for slot in logical_iter_mut(storage, self.shape(), self.strides(), self.offset()) {
            *slot = value.clone();
        }
        Ok(())
    }
}
```

### Step 3: 单元测试

参考 `docs/design/20-utility.md` 的「测试计划」为本 task 增加 fill 相关测试，覆盖基本填充、非连续布局和只读错误路径。

> **强制要求**:
> - 本步骤必须包含一个或者多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与Lint检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_fill_basic() {
        let mut tensor = Tensor1::<f64>::zeros([3]).unwrap();
        tensor.fill(2.5);
        assert_eq!(tensor.to_vec(), vec![2.5, 2.5, 2.5]);
    }

    #[test]
    fn test_try_fill_read_only_returns_error() {
        let tensor = Tensor1::from_shape_vec([2], vec![1_i32, 2]).unwrap();
        let mut view = tensor.view();
        let error = view.try_fill(7).unwrap_err();
        assert!(matches!(error, XenonError::InvalidStorageMode { .. }));
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| utility 只覆盖 clip / fill / contiguous | `20-utility §1.1` 与 §2 明确排除 sort/roll/shift 等其它杂项操作 |
| 所有数据访问必须按逻辑元素而非物理连续假设 | `20-utility §1.2` 与 §6 要求 stride-aware 处理非连续布局 |
| 连续化输出必须是 canonical F-order owned tensor | `20-utility §5.5` 依赖 `21-type §5.5` 的 canonical owned 承诺 |

## 验证方式

1. 编译检查: `cargo check`
2. Lint 检查: `cargo clippy --all-targets`
3. 测试验证: `cargo test test_fill`
4. 手动验证: 构造一个切片/转置得到的可写非连续视图，调用 `fill()` 后检查原张量对应逻辑位置被改写且 padding 未变化。

## 注意事项

- `fill()` 不返回 `Result`，因此只能暴露给 `S: StorageMut` 的编译期可写路径。
- `try_fill()` 必须把零步长广播只读结果视为不可写。
- 对 `value` 使用 `Clone`，不要要求 `Copy`，以保留所有 `Element` 类型的可用性。
