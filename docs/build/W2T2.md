# 实施方案: W2T2

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W2 — Error System |
| Task | W2T2 — XenonError enum + Result alias |
| 目标文件 | `src/error.rs` |
| 前置依赖 | W2T1（所有辅助枚举类型已定义） |
| 设计文档 | 26-error.md §5, §6, §7, §8 |
| 预估工时 | 12 min |

## 目标

XenonError enum definition with all structured variants + Result type alias.

在 `src/error.rs` 中追加定义：
- `XenonError` 枚举，包含所有错误变体，覆盖 FFI、Workspace、Type Conversion、Layout、Shape、Indexing、Construction、Broadcast 等各子系统的错误场景
- `Result<T>` 类型别名：`pub type Result<T> = core::result::Result<T, XenonError>`

## 前置准备

- W2T1 完成：所有辅助枚举（`FfiErrorCategory`, `FfiBackend`, `AbiMismatchKind`, `WorkspaceErrorCategory`, `WorkspaceBorrowKind`, `WorkspaceBorrowState`, `TypedViewRejection`, `ConversionFailureReason`, `InvalidArgumentKind`, `InvalidLayoutReason`, `StorageKindTag`, `StorageConversionKind`, `InvalidShapeKind`）已定义

## 实施步骤

### Step 1: 定义 XenonError 枚举

```rust
/// The canonical error type for all recoverable Xenon errors.
///
/// Each variant carries structured context fields describing the failure.
///
/// This enum is marked `#[non_exhaustive]`: downstream `match` expressions
/// MUST include a wildcard arm (`_ => ...`) and MUST NOT exhaustively pattern
/// against the listed variants.
#[derive(Debug, Clone, PartialEq)]
#[non_exhaustive]
pub enum XenonError {
    /// Two shapes are incompatible for the requested operation.
    ShapeMismatch {
        operation: Cow<'static, str>,
        left_shape: Vec<usize>,
        right_shape: Vec<usize>,
    },

    /// Broadcasting shapes are incompatible.
    BroadcastError {
        operation: Cow<'static, str>,
        lhs_shape: Vec<usize>,
        rhs_shape: Vec<usize>,
        /// Expected target shape, if one was computed.
        attempted_target_shape: Option<Vec<usize>>,
        axis: Option<usize>,
    },

    /// Reserved for future BLAS/FFI layout validation.
    LayoutMismatch {
        operation: Cow<'static, str>,
        required_layout: Cow<'static, str>,
        actual_layout: Cow<'static, str>,
        shape: Vec<usize>,
    },

    /// Invalid memory layout detected (construction, view, raw-parts, etc.).
    InvalidLayout {
        operation: Cow<'static, str>,
        storage_kind: StorageKindTag,
        shape: Vec<usize>,
        strides: Vec<usize>,
        offset: usize,
        storage_len: usize,
        reason: InvalidLayoutReason,
    },

    /// Axis index is out of the valid dimension range.
    InvalidAxis {
        operation: Cow<'static, str>,
        axis: usize,
        ndim: usize,
        shape: Vec<usize>,
    },

    /// Shape value invalid for the requested operation.
    InvalidShape {
        operation: Cow<'static, str>,
        shape: Vec<usize>,
        kind: InvalidShapeKind,
        /// The specific dimension that caused the failure, if identifiable.
        offending_dim: Option<usize>,
    },

    /// The number of dimensions does not match what the operation expects.
    DimensionMismatch {
        operation: Cow<'static, str>,
        expected: usize,
        actual: usize,
    },

    /// Generic invalid argument error with structured classification.
    InvalidArgument {
        operation: Cow<'static, str>,
        kind: InvalidArgumentKind,
    },

    /// Storage mode incompatible with the requested operation.
    InvalidStorageMode {
        operation: Cow<'static, str>,
        expected: StorageKindTag,
        actual: StorageKindTag,
        shape: Option<Vec<usize>>,
        conversion: Option<StorageConversionKind>,
    },

    /// FFI-related error (raw-parts, BLAS/LAPACK interoperability).
    Ffi {
        operation: Cow<'static, str>,
        category: FfiErrorCategory,
        backend: FfiBackend,
        /// Chained inner error, if this wraps a lower-level failure.
        cause: Option<Box<XenonError>>,
    },

    /// Workspace operation error (alloc, borrow, split, capacity).
    Workspace {
        operation: Cow<'static, str>,
        category: WorkspaceErrorCategory,
        /// Chained inner error, if this wraps a lower-level failure.
        cause: Option<Box<XenonError>>,
    },

    /// Multi-dimensional index out of bounds.
    IndexOutOfBounds {
        operation: Cow<'static, str>,
        /// The full attempted index (one component per axis).
        attempted_index: Vec<usize>,
        axis: usize,
        shape: Vec<usize>,
    },

    /// Element type conversion failed (e.g. `cast`, `Complex -> Real`).
    TypeConversion {
        operation: Cow<'static, str>,
        source_type: &'static str,
        target_type: &'static str,
        reason: ConversionFailureReason,
        element_index: Option<usize>,
    },
}
```

### Step 2: 定义 Result 类型别名

```rust
/// Canonical `Result` alias used by all public Xenon APIs.
///
/// Equivalent to `core::result::Result<T, XenonError>`.
pub type Result<T> = core::result::Result<T, XenonError>;
```

### Step 3: 单元测试

> **参考**: 设计文档 `26-error §8.2` 测试清单：`test_clone_eq_roundtrip`, `test_index_error_reports_axis_and_shape` 等。

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use alloc::borrow::Cow;

    /// Verify XenonError enum is constructable with each variant.
    #[test]
    fn test_error_variants_construct() {
        // ShapeMismatch
        let e = XenonError::ShapeMismatch {
            operation: Cow::Borrowed("dot"),
            left_shape: vec![2, 3],
            right_shape: vec![3, 4],
        };
        assert!(!format!("{:?}", e).is_empty());

        // IndexOutOfBounds
        let e = XenonError::IndexOutOfBounds {
            operation: Cow::Borrowed("slice"),
            attempted_index: vec![0, 5],
            axis: 1,
            shape: vec![3, 4],
        };
        if let XenonError::IndexOutOfBounds { axis, attempted_index, .. } = &e {
            assert_eq!(*axis, 1);
            assert_eq!(*attempted_index, vec![0, 5]);
        } else {
            panic!("variant mismatch");
        }

        // TypeConversion
        let e = XenonError::TypeConversion {
            operation: Cow::Borrowed("cast"),
            source_type: "f64",
            target_type: "i32",
            reason: ConversionFailureReason::FloatToInteger,
            element_index: None,
        };
        if let XenonError::TypeConversion { source_type, .. } = &e {
            assert_eq!(*source_type, "f64");
        } else {
            panic!("variant mismatch");
        }

        // DimensionMismatch
        let e = XenonError::DimensionMismatch {
            operation: Cow::Borrowed("reshape"),
            expected: 2,
            actual: 3,
        };
        if let XenonError::DimensionMismatch { expected, .. } = &e {
            assert_eq!(*expected, 2);
        } else {
            panic!("variant mismatch");
        }

        // Ffi with cause chain
        let inner = Box::new(XenonError::InvalidAxis {
            operation: Cow::Borrowed("check"),
            axis: 3,
            ndim: 2,
            shape: vec![2, 3],
        });
        let e = XenonError::Ffi {
            operation: Cow::Borrowed("export"),
            category: FfiErrorCategory::NullPointer {
                argument: Cow::Borrowed("data"),
            },
            backend: FfiBackend::RawParts,
            cause: Some(inner),
        };
        if let XenonError::Ffi { cause, .. } = &e {
            assert!(cause.is_some());
        } else {
            panic!("variant mismatch");
        }
    }

    /// Verify debug formatting doesn't panic for any variant.
    #[test]
    fn test_error_debug_no_panic() {
        let errors = [
            XenonError::ShapeMismatch {
                operation: Cow::Borrowed("reshape"),
                left_shape: vec![],
                right_shape: vec![1],
            },
            XenonError::InvalidShape {
                operation: Cow::Borrowed("from_shape_vec"),
                shape: vec![2, 3],
                kind: InvalidShapeKind::ElementCountMismatch {
                    expected: 6,
                    actual: 5,
                },
                offending_dim: None,
            },
            XenonError::BroadcastError {
                operation: Cow::Borrowed("add"),
                lhs_shape: vec![2, 1],
                rhs_shape: vec![3, 1],
                attempted_target_shape: None,
                axis: None,
            },
        ];
        for e in &errors {
            let _ = format!("{:?}", e);
        }
    }

    /// Verify Clone + PartialEq roundtrip consistency.
    /// Ref: 26-error §8.2 — test_clone_eq_roundtrip
    #[test]
    fn test_clone_eq_roundtrip() {
        let e1 = XenonError::InvalidAxis {
            operation: Cow::Borrowed("sum"),
            axis: 1,
            ndim: 2,
            shape: vec![3, 4],
        };
        let e2 = e1.clone();
        assert_eq!(e1, e2);

        let e3 = XenonError::InvalidAxis {
            operation: Cow::Borrowed("sum"),
            axis: 0,
            ndim: 2,
            shape: vec![3, 4],
        };
        assert_ne!(e1, e3);
    }

    /// Verify IndexOutOfBounds carries axis and shape context.
    /// Ref: 26-error §8.2 — test_index_error_reports_axis_and_shape
    #[test]
    fn test_index_error_reports_axis_and_shape() {
        let e = XenonError::IndexOutOfBounds {
            operation: Cow::Borrowed("index"),
            attempted_index: vec![2, 8],
            axis: 1,
            shape: vec![3, 4],
        };
        if let XenonError::IndexOutOfBounds { axis, shape, attempted_index, .. } = &e {
            assert_eq!(*axis, 1);
            assert_eq!(*shape, vec![3, 4]);
            assert_eq!(*attempted_index, vec![2, 8]);
        } else {
            panic!("variant mismatch");
        }
    }

    /// Verify Result type alias is usable.
    #[test]
    fn test_result_alias_usable() {
        let ok: Result<i32> = Ok(42);
        assert_eq!(ok.unwrap(), 42);

        let err: Result<i32> = Err(XenonError::DimensionMismatch {
            operation: Cow::Borrowed("test"),
            expected: 1,
            actual: 2,
        });
        assert!(err.is_err());
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| `#[non_exhaustive]` on `XenonError` | `26-error §5.1` — 允许未来小版本新增变体而不破坏下游 match |
| `Cow<'static, str>` 用于 `operation` | 既支持编译期静态字符串又支持动态构造 |
| 每个变体带有结构化字段 | `26-error §5.6` 要求结构化上下文字段 |
| `Vec<usize>` 而非 `&[usize]` | 错误需要拥有数据以跨线程传递 |
| `cause: Option<Box<XenonError>>` for Ffi/Workspace | `26-error §5.1` 链式错误模式，source() 可追溯内层原因 |
| `Result<T>` 与 `XenonError` 同任务定义 | `Result<T>` 直接依赖 `XenonError`，故必须在 `XenonError` 定义之后 |
| 测试使用 `if let` 解构而非 `e.field` 访问 | `XenonError` 变体字段非位置字段，命名字段需要 `match` / `if let` 提取 |

## 验证方式

1. **编译**: `cargo check`
2. **测试**: `cargo test -- error`
3. **调试输出**: 确认 `{:?}` 输出有意义的上下文信息
4. **Clone/PartialEq**: 确保错误可克隆和比较
5. **Result 别名**: 确认 `Result<T>` 可以替代 `core::result::Result<T, XenonError>`

## 注意事项

- 此任务定义 `XenonError` 主枚举和 `Result<T>` 类型别名；Display/Error impl 由 W2T3/W2T4 负责
- 所有辅助类型（如 `FfiErrorCategory`, `StorageKindTag`, `InvalidLayoutReason` 等）**必须在 W2T1 中已定义**，否则本任务编译失败
- `#[non_exhaustive]` 意味着下游 match 必须包含通配分支 `_ => ...`
- 后续 task 引用 `XenonError` / `Result` 时无需额外导入（在 crate root 中）
