# 实施方案: W11T10

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W11 — Broadcasting |
| Task | W11T10 — Integration tests |
| 目标文件 | `tests/test_broadcast.rs` |
| 前置依赖 | W11T8, W11T9 |
| 设计文档 | 15-broadcast §5, §6, §7, §8 |
| 预估工时 | 10 min |

## 目标

Integration tests。

本任务在 `tests/test_broadcast.rs` 中完成对应实现，遵循 `SUMMARY.md` 对 W11T10 的粒度约束，并与 `15-broadcast.md` 中的公共 API、语义约束、测试计划保持一致。

## 前置准备

- W3/W6/W8 已完成：`Dimension`、`BroadcastDim`、layout flags、`TensorBase` 与只读 view 构造入口可用。
- W2 已完成：`XenonError::BroadcastError` 与参数错误可用于结构化失败返回。

## 实施步骤

### Step 1: 创建集成测试文件

按照 `15-broadcast.md` 的接口边界实现本步骤，保持单文件内最小修改。

```rust
use xenon::broadcast::{broadcast_shape, broadcast_strides, can_broadcast};
use xenon::prelude::*;

#[test]
fn test_broadcast_shape_basic() {
    let shape = broadcast_shape(&[1, 3], &[2, 3]).unwrap();
    assert_eq!(shape.as_slice(), &[2, 3]);
}
```
### Step 2: 覆盖 view 与零拷贝语义

按照 `15-broadcast.md` 的接口边界实现本步骤，保持单文件内最小修改。

```rust
#[test]
fn test_broadcast_to_basic() {
    let tensor = Tensor2::from_vec([1, 3], vec![1.0, 2.0, 3.0]).unwrap();
    let view = tensor.broadcast_to([2, 3]).unwrap();
    assert_eq!(view.shape(), &[2, 3]);
    assert_eq!(view.strides()[0], 0);
    assert_eq!(view.as_ptr(), tensor.as_ptr());
}
```
### Step 3: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或者多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与Lint检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_broadcast_high_rank_ixdyn() {
        let shape = broadcast_shape(&[2, 1, 4], &[3, 2, 5, 4]).unwrap();
        assert_eq!(shape.as_slice(), &[3, 2, 5, 4]);
    }
    
    #[test]
    fn test_broadcast_read_only() {
        let tensor = Tensor2::zeros([1, 3]);
        let view = tensor.broadcast_to([2, 3]).unwrap();
        let _ = view.iter().count();
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| 广播按尾轴右对齐逐轴判定 | 15-broadcast §1.2 和 §5.2 要求与 Numpy 规则一致 |
| 广播视图始终只读且零拷贝 | 15-broadcast §1.1、§6.1 明确禁止可写广播视图 |
| 非空零步长视图重算为 `BroadcastView` | 15-broadcast §8.2 和 §9.1 要求 flags 与零步长语义一致 |

## 验证方式

1. 编译检查: `cargo check`
2. Lint 检查: `cargo clippy --all-targets`
3. 测试验证: `cargo test broadcast`
4. 手动验证: 编写一个 `[1, 3] -> [2, 3]` 的广播 view，确认 shape 为 `[2, 3]`、广播轴 stride 为 `0`、data pointer 与源张量一致。

## 注意事项

- 空轴广播允许存在，但空数组退化零步长不应错误标记为非空 `BroadcastView`。
- `broadcast_to()` 不得复制底层存储，也不得返回任何可写 view。
- `broadcast_with()` 的类型 bound 必须与 15-broadcast §5.1 的双向 `BroadcastDim` 约束保持一致。
