# 实施方案: W13T4

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W13 — FFI Helpers |
| Task | W13T4 — ptr.rs: `export()` / `export_mut()` inherent methods on `TensorBase` + re-export `OwnedRawParts` / `TensorBase` |
| 目标文件 | `src/ffi/ptr.rs` 和 `src/ffi/mod.rs`（追加 `pub use`）|
| 前置依赖 | W13T3，W8T6 (`as_storage_ptr` / `as_mut_ptr`)，W8T7 (`from_raw_parts*` / `into_raw_parts` / `from_raw_parts_owned` / `OwnedRawParts`)|
| 设计文档 | 23-ffi §5.4（`export` / `export_mut` 实现）、§5.7（`from_raw_parts*` 归属 07-tensor）、§5.8（`OwnedRawParts` re-export）|
| 预估工时 | 10 min |

## 目标

在 `src/ffi/ptr.rs` 中：

1. 实现 `TensorBase` 的 `export()` 公开方法（`S: Storage`，返回 `TensorExportRaw`），通过私有 `export_internal()` 中转。
2. 实现 `TensorBase` 的 `export_mut()` 公开方法（`S: StorageMut`，返回 `TensorExportMutRaw`），通过私有 `export_mut_internal()` 中转。
3. Re-export `OwnedRawParts` 与 `TensorBase`（来自 `crate::tensor`），让 FFI 消费者可通过 `crate::ffi::*` 一站式访问。
4. 在 `src/ffi/mod.rs` 末尾追加对应 `pub use` 行。

> **重要 API 形式说明**：`from_raw_parts` / `from_raw_parts_mut` / `into_raw_parts` / `from_raw_parts_owned` 是 `TensorBase` 的 inherent methods（定义在 `07-tensor.md` §5.7 / `src/tensor/construct.rs`），**没有自由路径符号可被 `pub use`**。`crate::ffi` 仅需 re-export `OwnedRawParts` 与 `TensorBase` 类型，方法随类型自动可达（与 `23-ffi.md` §5.8 line 758-764 一致）。

## 前置准备

- **W8T6 已完成**：`TensorBase::as_storage_ptr(&self) -> *const A` / `as_storage_mut_ptr(&mut self) -> *mut A` / `as_ptr` / `as_mut_ptr` / `is_empty` / `ndim` / `shape` / `strides` / `storage_len` / `offset` 可用。
- **W8T7 已完成**：`from_raw_parts*` / `into_raw_parts` / `from_raw_parts_owned` 已实现，`OwnedRawParts` 类型已导出至 `crate::tensor`。
- **W13T3 已完成**：`super::private::{TensorExport, TensorExportMut}` 与 `From` 转换可用。
- **W13T2 已完成**：`super::types::{TensorExportRaw, TensorExportMutRaw}` 可用。
- **W3T2 / W4T1 已完成**：`crate::element::{Element, ElementType, element_type_of}` 可用。

## 实施步骤

### Step 1: ptr.rs 头部 use 与 re-export

```rust
//! FFI pointer-export API and raw-parts re-exports.
//!
//! See `docs/design/23-ffi.md` §5.4 (`export` / `export_mut`) and §5.7-§5.8
//! (raw-parts ownership semantics) for the authoritative contract.

use core::marker::PhantomData;
use core::ptr::NonNull;

use super::private::{TensorExport, TensorExportMut};
use super::types::{TensorExportMutRaw, TensorExportRaw};
use crate::dimension::Dimension;
use crate::element::{element_type_of, Element};
use crate::storage::{Storage, StorageMut};

/// Re-exports for FFI consumers to access raw-parts metadata and the
/// tensor type via the stable `crate::ffi::*` path.
///
/// `OwnedRawParts` is defined in `crate::tensor` (see `07-tensor.md`
/// §5.7). The inherent methods `from_raw_parts` / `from_raw_parts_mut`
/// / `into_raw_parts` / `from_raw_parts_owned` are defined on
/// `TensorBase` and ride on the type itself — they become callable as
/// soon as `TensorBase` is in scope, with no extra `pub use` required
/// (inherent methods have no free-function path symbol).
///
/// Note: a single `pub use` brings `TensorBase` into the current scope
/// (so that `impl TensorBase { ... }` below works) **and** simultaneously
/// re-exports it from `crate::ffi::ptr`.
pub use crate::tensor::{OwnedRawParts, TensorBase};
```

### Step 2: 实现 `export()` —— `impl<S, D, A> TensorBase<S, D>` 中的 inherent method

按 `23-ffi.md` §5.4 line 530-585 完整复刻 —— `data` 字段持 **storage base pointer**（非 logical first），空张量返回 `NonNull::dangling()`。

```rust
impl<S, D, A> TensorBase<S, D>
where
    S: Storage<Elem = A>,
    A: Element,
    D: Dimension,
{
    /// Export tensor data as a C-compatible raw structure.
    ///
    /// The returned `TensorExportRaw` borrows the tensor's data and
    /// metadata. The consumer must ensure the tensor outlives the export.
    /// This method does not fail; it always returns a valid export.
    ///
    /// `data` always carries the **storage base pointer**; the logical
    /// first element address is derived from `data.add(offset)` for
    /// non-empty tensors. Empty tensors (`len() == 0`) get a valid
    /// aligned `dangling` pointer that must not be dereferenced;
    /// `shape`, `strides`, and `offset` still describe the empty
    /// tensor metadata.
    ///
    /// **Visibility & return type** (`23-ffi.md` §5.4 line 547-555):
    /// This is the public FFI entry; it returns `TensorExportRaw` (the
    /// C-visible non-generic raw descriptor). The intermediate generic
    /// descriptor `TensorExport<'_, A>` is `pub(crate)` Rust-only
    /// borrowing evidence and cannot appear in a `pub fn` return type.
    pub fn export(&self) -> TensorExportRaw {
        self.export_internal().into()
    }

    /// `pub(crate)` internal helper: produces the typed generic descriptor
    /// for in-crate borrow tracking and lifetime evidence. Not exposed to
    /// downstream consumers; use `export()` for the public FFI surface.
    pub(crate) fn export_internal(&self) -> TensorExport<'_, A> {
        let data = if self.is_empty() {
            // Empty tensor: a valid aligned non-dereferenceable pointer.
            // Do NOT call as_storage_ptr() — the backing storage may be
            // empty or even unallocated (e.g. zero-cap Vec).
            NonNull::<A>::dangling().as_ptr() as *const A
        } else {
            self.as_storage_ptr()
        };
        TensorExport {
            data,
            element_type: element_type_of::<A>(),
            ndim: self.ndim(),
            shape: self.shape().as_ptr(),
            strides: self.strides().as_ptr(),
            storage_len: self.storage_len(),
            offset: self.offset(),
            _marker: PhantomData,
        }
    }
}
```

### Step 3: 实现 `export_mut()` —— `impl<S, D, A> TensorBase<S, D> where S: StorageMut` 中的 inherent method

按 `23-ffi.md` §5.4 line 587-631 复刻。

```rust
impl<S, D, A> TensorBase<S, D>
where
    S: StorageMut<Elem = A>,
    A: Element,
    D: Dimension,
{
    /// Export tensor data with mutable access.
    ///
    /// **Visibility & return type**: Public FFI entry; returns
    /// `TensorExportMutRaw`. The intermediate generic
    /// `TensorExportMut<'_, A>` is `pub(crate)` Rust-only borrowing
    /// evidence and cannot appear in `pub fn` return type.
    ///
    /// This API is only implemented for writable storage, so read-only
    /// storage modes are rejected at the trait boundary rather than at
    /// runtime. No additional fallible validation is performed beyond the
    /// existing `&mut self` + `S: StorageMut` exclusivity boundary.
    pub fn export_mut(&mut self) -> TensorExportMutRaw {
        self.export_mut_internal().into()
    }

    /// `pub(crate)` internal helper: produces the typed mutable generic
    /// descriptor for in-crate borrow tracking and lifetime evidence.
    pub(crate) fn export_mut_internal(&mut self) -> TensorExportMut<'_, A> {
        let data = if self.is_empty() {
            NonNull::<A>::dangling().as_ptr()
        } else {
            self.as_storage_mut_ptr()
        };
        // Capture metadata *before* moving the mutable borrow into `data`.
        let element_type = element_type_of::<A>();
        let ndim = self.ndim();
        let shape = self.shape().as_ptr();
        let strides = self.strides().as_ptr();
        let storage_len = self.storage_len();
        let offset = self.offset();
        TensorExportMut {
            data,
            element_type,
            ndim,
            shape,
            strides,
            storage_len,
            offset,
            _marker: PhantomData,
        }
    }
}
```

> `export_mut_internal` 中 `data` 来自 `&mut self`、其余字段来自 `&self`。Rust 借用检查器要求先取 `*mut` 数据指针、再单独按 `&self` 取 metadata（或反之）。若编译报借用冲突，按 §5.4 line 615-630 的字面顺序（`data` 先、其余后），并通过临时变量 `let element_type = ...;` 等先求值后构造结构体。

### Step 4: 在 mod.rs 末尾追加 re-exports

```rust
// src/ffi/mod.rs (append at end after W13T2's pub use)
pub use ptr::{OwnedRawParts, TensorBase};
```

> 同时把 W13T4 的 `pub use crate::tensor::{OwnedRawParts, TensorBase};` 留在 `ptr.rs` 顶部即可；mod.rs 通过 `pub use ptr::*` 形式让两个类型在 `crate::ffi` 命名空间下可达。这就完成了 `23-ffi.md` §5.8 line 758-764 要求的 re-export 路径。

### Step 5: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与 Lint 检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

测试函数命名对齐 `23-ffi.md` §8.2 单元测试清单 line 1359-1360（`test_export_contract` / `test_export_mut_contract`），并补充 `data == as_storage_ptr()` 的关键断言（防止再次出现 `data = as_ptr()` 这种 logical-first vs storage-base 混淆）和空张量 dangling 路径。

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::tensor::Tensor;
    use crate::dimension::Ix1;

    /// §8.2 test_export_contract — exports match source metadata, and
    /// crucially `data == as_storage_ptr()` (NOT `as_ptr()`).
    #[test]
    fn test_export_contract_data_is_storage_base() {
        let tensor: Tensor<i32, Ix1> =
            Tensor::from_shape_vec([3], vec![10_i32, 20, 30]).unwrap();
        let raw = tensor.export();
        assert_eq!(raw.ndim, 1);
        assert_eq!(raw.storage_len, 3);
        assert_eq!(raw.element_type, ElementType::I32);
        // The critical contract: §5.4 mandates `data` carries the
        // storage base pointer, not the logical first.
        assert_eq!(raw.data as *const i32, tensor.as_storage_ptr());
    }

    /// Empty tensors must export a dangling but valid aligned pointer.
    #[test]
    fn test_export_empty_tensor_dangling() {
        let tensor: Tensor<f64, Ix1> =
            Tensor::from_shape_vec([0], Vec::new()).unwrap();
        let raw = tensor.export();
        assert_eq!(raw.ndim, 1);
        assert_eq!(raw.storage_len, 0);
        // dangling pointers are non-null and properly aligned.
        assert!(!raw.data.is_null());
        assert_eq!(
            (raw.data as usize) % core::mem::align_of::<f64>(),
            0
        );
    }

    /// §8.2 test_export_mut_contract — `data` writable; only callable
    /// on `StorageMut`. We verify writability by storing through the raw
    /// pointer and reading back via the original tensor.
    #[test]
    fn test_export_mut_contract_writable() {
        let mut tensor: Tensor<i32, Ix1> =
            Tensor::from_shape_vec([2], vec![1_i32, 2]).unwrap();
        let storage_base_before = tensor.as_storage_ptr() as usize;
        let raw = tensor.export_mut();
        assert_eq!(raw.ndim, 1);
        assert_eq!(raw.data as usize, storage_base_before);
        // SAFETY: raw.data is the storage base; storage_len=2; we write
        // within the borrowed range while no other reference exists
        // (raw_mut consumed `&mut tensor` for its lifetime).
        unsafe {
            (raw.data as *mut i32).add(0).write(99);
        }
        // Reading back through `tensor` is sound after `raw` is dropped
        // because `TensorExportMutRaw` does not own the lifetime —
        // the borrow ends at end-of-expression for `raw`.
        drop(raw);
        let observed = tensor.as_slice().unwrap()[0];
        assert_eq!(observed, 99);
    }
}
```

### Step 6: 编译期 (compile_fail) 测试

按 `23-ffi.md` §8.7 line 1408-1410 要求：

- `into_raw_parts()` 仅对 `Owned` 存储开放（验证由 W8T7 提供，但 W13T4 通过 `ptr.rs` 暴露入口，应再次验证 re-export 路径不会意外放宽约束）。
- `export_mut()` 仅对 `S: StorageMut` 路径开放。

```rust
/// Doctest enforcing that `export_mut` cannot be called on a read-only view.
///
/// ```compile_fail
/// use xenon::tensor::Tensor;
/// use xenon::dimension::Ix1;
/// let t: Tensor<i32, Ix1> = Tensor::from_shape_vec([2], vec![1, 2]).unwrap();
/// let v = t.view();
/// // `view()` returns a `ViewRepr`-backed TensorBase, which is `Storage`
/// // but NOT `StorageMut`. `export_mut` must therefore fail to compile.
/// let _ = (&v).export_mut();
/// ```
fn _doctest_export_mut_rejects_view() {}

/// Doctest enforcing that `into_raw_parts` cannot be called on a view.
///
/// ```compile_fail
/// use xenon::tensor::Tensor;
/// use xenon::dimension::Ix1;
/// let t: Tensor<i32, Ix1> = Tensor::from_shape_vec([2], vec![1, 2]).unwrap();
/// let v = t.view();
/// // `into_raw_parts` is only implemented for Owned storage.
/// let _ = v.into_raw_parts();
/// ```
fn _doctest_into_raw_parts_rejects_view() {}
```

> `compile_fail` doctest 块用 `///`（文档注释）挂在 dummy fn 上，`cargo test --doc` 会运行它们。这避免引入 trybuild 等额外 dev-dependency（W5T13 同款策略）。

## 关键设计决策

| 决策 | 依据 |
|------|------|
| `export()` / `export_mut()` 实现为 `TensorBase` 的 inherent methods | `23-ffi.md` §5.4 line 530-585 / 587-631 明确写为 `impl TensorBase { pub fn export(&self)... }`；调用形式必须是 `tensor.export()`，而非 `ffi::export(&tensor)` |
| `data` 字段持 storage base pointer，非 logical first | `23-ffi.md` §5.4 line 542-543, 568-575 反复强调：`data always carries the storage base pointer; the logical first element address is derived from data.add(offset)` |
| 空张量返回 `NonNull::<A>::dangling().as_ptr()` 而非调用 `as_storage_ptr()` | `23-ffi.md` §5.4 line 568-575：空张量底层 storage 可能未分配（zero-cap Vec），直接取基址会触发 UB |
| 通过 `export_internal()` / `export_mut_internal()` 中转 generic descriptor | `23-ffi.md` §5.4 line 547-555：`pub fn export` 返回类型不能包含 `pub(crate)` 的 `TensorExport`（违反 `private_in_public`），用 pub(crate) 内部方法封装 + `From` 转换满足 Rust 可见性规则 |
| Re-export 仅覆盖 `OwnedRawParts` 与 `TensorBase` 类型 | `23-ffi.md` §5.8 line 758-764：`from_raw_parts*` / `into_raw_parts` / `from_raw_parts_owned` 是 `TensorBase` 的 inherent methods，没有自由路径符号；类型 re-export 后方法天然可达 |
| 用 `compile_fail` doctest 而非 trybuild 做编译期负向测试 | 与 W5T13 项目级策略一致；避免引入新的 dev-dependency；`cargo test --doc` 在 CI 中已被覆盖 |

## 验证方式

1. 编译检查: `cargo check`
2. Lint 检查: `cargo clippy --all-targets`
3. 测试验证: `cargo test ffi::ptr`（运行时测试） + `cargo test --doc -p xenon ffi::ptr`（compile_fail 测试）
4. 手动验证: 创建一个 sliced view（非零 `offset`），分别调用 `export()` 与 `as_storage_ptr()`、`as_ptr()` 比对：
   - `export().data` 等于 `as_storage_ptr()`（不是 `as_ptr()`）
   - `export().offset` 等于 `tensor.offset()`
   - 等价地：`(export().data as *const A).add(export().offset) == as_ptr()`

## 注意事项

- `from_raw_parts` / `from_raw_parts_mut` / `into_raw_parts` / `from_raw_parts_owned` **没有** `pub fn xxx<...>(tensor: ...)` 形式的自由函数，禁止尝试用 `pub use crate::tensor::from_raw_parts;` 之类写法 —— 会编译失败（inherent method 无独立路径）。
- 测试中不得使用不存在的 `Tensor1::from_vec([2], vec![..])` 形式：`from_vec(data)` 仅接受 1 个参数（参见 `18-construction.md` line 462-473）。需要显式 shape 时使用 `Tensor::from_shape_vec(shape, data)`。
- `export_mut_internal()` 中获取 `data` 与其他字段时注意借用顺序：先取 metadata 到局部变量、再取 `as_storage_mut_ptr()`，或反之；不要在同一表达式中混合 `&mut self` 与 `&self`。
- 不在本 task 修改 `src/prelude.rs`：`crate::ffi::*` 已经是稳定路径，与 W2T5 prelude 策略一致。
