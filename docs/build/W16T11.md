# 实施方案: W16T11

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W16 — Math Operations |
| Task | W16T11 — SIMD backend unified dispatch integration for math module |
| 目标文件 | `src/math/binary.rs, unary.rs, comparison.rs, src/simd/vector.rs` |
| 前置依赖 | W16T3, W14 |
| 设计文档 | 11-math.md |
| 预估工时 | 10 min |

## 目标

SIMD backend unified dispatch integration for math module。

本任务在 `src/math/binary.rs, unary.rs, comparison.rs, src/simd/vector.rs` 中完成对应实现切片，严格遵守 11-math §5, §6, §7, §8 的范围边界：先保证标量语义、错误边界与 feature gate 正确，再接入可选加速或模块导出。示例代码按当前任务展示核心实现形态，依赖 SUMMARY 中已完成的前置任务提供的 Tensor、Dimension、Element、Error、Dispatch 等类型。

## 前置准备

- 确认前置任务已完成：W16T3, W14。
- 阅读 `src/math/binary.rs, unary.rs, comparison.rs, src/simd/vector.rs` 所属模块现有代码，避免重写相邻任务已实现的逻辑。
- 对照 11-math §5, §6, §7, §8 的 API、内部实现和测试计划，确认本任务只修改目标文件列出的范围。
- 若涉及 feature gate，分别准备默认特性和相关特性启用后的验证命令。

## 实施步骤

### Step 1: Call `dispatch::select_exec_path` from math entry points after semantic validation

Call `dispatch::select_exec_path` from math entry points after semantic validation.

```rust
use crate::dispatch::{select_exec_path, ExecPath, ParallelGuard};

fn apply_unary_with_dispatch<A, S, D, F>(
    input: &TensorBase<S, D>,
    op: F,
) -> Tensor<A, D>
where
    A: Element,
    S: Storage<Elem = A>,
    D: Dimension,
    F: FnMut(A) -> A + Copy + Send + Sync + 'static,
{
    let (path, guard) = select_exec_path(input.as_slice().unwrap().len());
    match path {
        ExecPath::Serial | ExecPath::Simd => {
            // SIMD backend performs admission internally; falls back
            // to scalar on unconvertible byte patterns.
            simd::apply_unary_simd(input.slice(), op)
                .unwrap_or_else(|_| apply_unary_scalar(input, op))
        }
        ExecPath::Parallel => {
            // Parallel backend owns the guard; each worker chunk may
            // independently attempt SIMD admission.
            parallel::par_apply_unary(input, op, guard.unwrap())
        }
    }
}
```

### Step 2: Use SIMD backend facades only for supported operations and types

Use SIMD backend facades only for supported operations and types.

### Step 3: Pass `ParallelGuard` by value to parallel backend when `ExecPath::Parallel` is selected

Pass `ParallelGuard` by value to parallel backend when `ExecPath::Parallel` is selected.

### Step 4: Fall back to scalar loops when SIMD admission or operation coverage returns false

Fall back to scalar loops when SIMD admission or operation coverage returns false.

### Step 5: 单元测试

添加覆盖设计文档测试计划的单元测试，至少包含正常路径和一个边界或回退场景。

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::Tensor;

    #[test]
    fn test_add_simd_vs_scalar() {
        // Verify SIMD path produces identical results to scalar path.
        let a = Tensor::<f64, Ix1>::from_shape_vec(
            [256], (0..256).map(|x| x as f64).collect(),
        ).unwrap();
        let b = Tensor::<f64, Ix1>::from_shape_vec(
            [256], (0..256).map(|x| (x * 2) as f64).collect(),
        ).unwrap();
        let scalar_result = a.add(&b).unwrap(); // scalar path
        let simd_result = add_with_simd(&a, &b).unwrap(); // SIMD path
        for i in 0..256 {
            assert!(
                (scalar_result[[i]] - simd_result[[i]]).abs() < 1e-10,
                "SIMD and scalar result mismatch at index {}", i
            );
        }
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| 只实现本 task 对应切片 | SUMMARY.md 将 W14-W16 拆成单文件粒度任务，避免跨 task 重构 |
| Math performs semantic validation before dispatch; SIMD and parallel are acceleration paths only. | 11-math §5, §6, §7, §8 |
| 单元测试与实现同任务提交 | TEMPLATE.md 要求最后一个实施步骤必须包含单元测试 |

## 验证方式

1. 编译检查: `cargo check`
2. Feature 编译: 根据任务范围运行 `cargo check --features simd`、`cargo check --features parallel` 或两者组合
3. 测试验证: `cargo test --features simd,parallel test_add_simd_vs_scalar`
4. 回归验证: `cargo test`，涉及可选后端时再运行对应 feature 的测试命令
5. 手动验证: 阅读生成的 rustdoc 或测试输出，确认公开/内部 API 路径与预期模块路径一致

## 注意事项

- Math methods define user-visible semantics; acceleration backends cannot change shape, errors, NaN behavior, or panic boundaries.
- Broadcast incompatibility is recoverable, while integer overflow and division by zero are panic paths.
- Do not expose generic map helpers as public API in this wave.
- 所有代码注释和 Rust 标识符使用英文，文档说明保持当前实施方案风格。
- 不添加新的第三方依赖，除非设计文档已在 Cargo feature 中声明。
