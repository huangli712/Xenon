# 实施方案: W11T4

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W11 — Broadcasting |
| Task | W11T4 — can_broadcast(): trailing-axis alignment compatibility check |
| 目标文件 | `src/broadcast/shape.rs` |
| 前置依赖 | W11T2（shape.rs 骨架与 `can_broadcast` 签名占位） |
| 设计文档 | 15-broadcast.md §5.2 line 166（语义）、§6.2（算法伪码） |
| 预估工时 | 10 min |

## 目标

替换 W11T2 在 `src/broadcast/shape.rs` 中放置的 `can_broadcast` `todo!()` 骨架，实现"从尾轴对齐逐轴判定 Numpy 广播兼容性"的算法。

本 task **不**依赖 `broadcast_shape`（W11T5 完成）—— 采用独立尾轴对齐循环实现；W11T5 完成 `broadcast_shape` 后会把 `can_broadcast` 进一步简化为 `broadcast_shape(...).is_ok()`（保留同一函数名与签名）。

## 前置准备

- W11T2 已完成：`src/broadcast/shape.rs` 已存在；`can_broadcast` / `broadcast_shape` / `broadcast_strides` 三个签名占位 + `broadcast_error` helper 已就位。
- 本 task **不**修改 helper、不修改其他两个函数。

## 实施步骤

### Step 1: 实现右对齐尾轴兼容判断

按 §6.2 line 220-226 伪码 + §5.2 line 166（"仅回答兼容性，不分配、不生成中间结果"）—— 不分配 `Vec`、不抛错误，纯比较返回 bool：

```rust
/// Numpy-style broadcast compatibility check. From `15-broadcast.md §5.2` line 166
/// and §6.2 line 220-226: align dimensions from right; treat missing leading
/// dimensions as 1; two aligned dimensions are compatible if equal or one is 1.
pub fn can_broadcast(shape_a: &[usize], shape_b: &[usize]) -> bool {
    let ndim = shape_a.len().max(shape_b.len());
    for axis_from_back in 0..ndim {
        // Missing leading axes are treated as length 1 per §6.2 step 2.
        let a = shape_a
            .len()
            .checked_sub(axis_from_back + 1)
            .map(|i| shape_a[i])
            .unwrap_or(1);
        let b = shape_b
            .len()
            .checked_sub(axis_from_back + 1)
            .map(|i| shape_b[i])
            .unwrap_or(1);
        if a != b && a != 1 && b != 1 {
            return false;
        }
    }
    true
}
```

### Step 2: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或者多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与Lint检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

测试覆盖 §8.2 中 `test_can_broadcast_compatible` / `test_can_broadcast_incompatible` 两项，并附加 §8.3 边界场景（空 shape / 空轴 `1 -> 0`）：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_can_broadcast_compatible() {
        // Same shape.
        assert!(can_broadcast(&[2, 3], &[2, 3]));
        // Right-align broadcasting (`1` axis expands).
        assert!(can_broadcast(&[1, 3], &[2, 3]));
        assert!(can_broadcast(&[2, 3], &[1, 3]));
        // Missing leading axis treated as 1 (scalar to high-dim).
        assert!(can_broadcast(&[], &[2, 3]));
        assert!(can_broadcast(&[3], &[2, 3]));
        // Cross-rank: §8.3 high-rank scenario.
        assert!(can_broadcast(&[2, 1, 4], &[3, 2, 5, 4]));
    }

    #[test]
    fn test_can_broadcast_incompatible() {
        assert!(!can_broadcast(&[2, 3], &[4, 3]));
        assert!(!can_broadcast(&[2, 3, 4], &[2, 3, 5]));
    }

    /// §8.3 empty-axis: `[0, 3]` and `[1, 3]` are compatible (output shape `[0, 3]`).
    /// Compatibility judgment must not special-case axes of length 0.
    #[test]
    fn test_can_broadcast_empty_axis() {
        assert!(can_broadcast(&[0, 3], &[1, 3]));
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| 尾轴右对齐 + 缺失前导轴按 1 处理 | 15-broadcast §6.2 line 220-226 算法伪码 |
| 不分配 / 不抛错误 / 纯比较 | 15-broadcast §5.2 line 166："仅回答兼容性，不分配、不生成中间结果" |
| 不依赖 `broadcast_shape`（独立实现）；W11T5 后再简化为复用 | 避免循环引用；W11T5 完成后由 §5.2 line 166 隐含的"语义等价"重写为 `broadcast_shape(...).is_ok()` |

## 验证方式

1. 编译检查: `cargo check`
2. Lint 检查: `cargo clippy --all-targets`
3. 测试验证: `cargo test --lib broadcast::shape::tests::test_can_broadcast_compatible broadcast::shape::tests::test_can_broadcast_incompatible broadcast::shape::tests::test_can_broadcast_empty_axis`
4. 手动验证: REPL 中调用 `xenon::broadcast::can_broadcast(&[2, 1, 4], &[3, 2, 5, 4])` 应返回 `true`；`(&[2, 3], &[4, 3])` 应返回 `false`。

## 注意事项

- 本 task **只**实现 `can_broadcast`，**不**触碰 `broadcast_shape` / `broadcast_strides` / `broadcast_error`。
- 空轴广播（`[0, 3]` 与 `[1, 3]`）必须判定为兼容；`§8.3` 边界表第 1 行明确该语义。
- 不要在本 task 引入对 `broadcast_shape` 的调用 —— 它在 W11T5 完成前是 `todo!()`，调用会 panic。
