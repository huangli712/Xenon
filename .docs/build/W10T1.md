# 实施方案: W10T1

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W10 — Dispatch |
| Task | W10T1 — Module skeleton: ExecPath enum, ParallelExecStrategy struct, module docs |
| 目标文件 | `src/dispatch.rs` |
| 前置依赖 | None |
| 设计文档 | 30-dispatch.md |
| 预估工时 | 15 min |

## 目标

Module skeleton: ExecPath enum, ParallelExecStrategy struct, module docs。
本任务将 SUMMARY.md 中的目标落实到 `src/dispatch.rs`，并与 `30-dispatch §5, §6, §7, §8` 的 API、实现拆分和测试计划保持一致。

## 前置准备

- 确认 W8 tensor 查询方法可提供 len、contiguous 与 alignment 信息。
- 确认 dispatch 为 `src/dispatch.rs` 单文件模块。
- 按 `30-dispatch.md §5` 保持 API 为 crate 内部使用。

## 实施步骤

### Step 1: 实现核心代码

在 `src/dispatch.rs` 中添加本任务要求的最小实现，保持职责边界与设计文档一致。本骨架需满足三个要点：

1. `ExecPath` 三 variant 的 doc comments 严格落地 `30-dispatch.md §5.2`（每个 variant 的语义、与 `simd/` / `parallel/` 后端的职责分工、与 guard 绑定的不变量提示）。
2. `ParallelExecStrategy` 字段顺序、字段名 (`chunk_size`, `max_workers`) 与可见性按 `30-dispatch.md §5.3`。
3. `ParallelExecStrategy` 仅在 `feature = "parallel"` 下编译（`30-dispatch.md §5.1`：非 parallel feature 下 `new()` 不可达，dispatch 永不返回 `ExecPath::Parallel`）。

```rust
//! Internal execution-path dispatch.
//!
//! See `docs/design/30-dispatch.md` for the full design (path
//! arbitration, nested-parallel guard, threshold storage, feature
//! gates). All items in this module are `pub(crate)`.

/// Three mutually exclusive execution paths recommended by dispatch.
///
/// See `30-dispatch.md §5.2` for full per-variant semantics.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub(crate) enum ExecPath {
    /// Serial scalar execution. Default fallback when neither SIMD
    /// nor parallel preconditions are met.
    Serial,
    /// Serial path with SIMD acceleration. dispatch only signals
    /// "SIMD path is preferred"; the `simd/` backend retains final
    /// admission (ISA, lane width, alignment per `08-simd.md §5.7`).
    Simd,
    /// Parallel execution. Returned only when `feature = "parallel"`
    /// is enabled, the input meets the parallel threshold, and the
    /// current thread is not already inside a library-internal
    /// parallel region. Always accompanied by `Some(ParallelGuard)`
    /// per `30-dispatch.md §5.5`.
    Parallel,
}

/// Execution strategy parameters consumed by the parallel backend.
///
/// Defined here; consumed by `parallel/` module functions such as
/// `par_map`, `par_zip_map`, `par_sum`, `par_dot`. Fields are private
/// to enforce construction via `ParallelExecStrategy::new()` (see
/// `30-dispatch.md §5.3`).
///
/// Only compiled under `feature = "parallel"` per §5.1: outside that
/// feature `select_exec_path()` can never return `ExecPath::Parallel`,
/// so the strategy type itself is unreachable.
#[cfg(feature = "parallel")]
#[derive(Debug, Clone, Copy)]
pub(crate) struct ParallelExecStrategy {
    /// Suggested chunk size for parallel chunking. `None` means the
    /// parallel backend decides (typically via `compute_safe_chunks`).
    chunk_size: Option<usize>,
    /// Maximum worker count. `None` means use rayon's default thread
    /// pool size.
    max_workers: Option<usize>,
}
```

### Step 2: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或者多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与Lint检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

测试依据: `test_exec_path_enum_defined` 作为骨架可编译测试，验证枚举类型可引用与比较。

```rust
#[cfg(test)]
mod tests {
    use super::ExecPath;

    #[test]
    fn test_exec_path_enum_defined() {
        // Skeleton compile-smoke: verifies the enum type is usable.
        // Real threshold-vs-path behaviour is tested in W10T4/W10T6.
        assert_eq!(ExecPath::Serial, ExecPath::Serial);
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| ExecPath variant doc comments | `30-dispatch.md §5.2` 三 variant 各自语义 + 后端职责分工 |
| ParallelExecStrategy 字段：`chunk_size` / `max_workers` | `30-dispatch.md §5.3` 显式规定的字段名与顺序 |
| `ParallelExecStrategy` 限 `#[cfg(feature = "parallel")]` | `30-dispatch.md §5.1` 非 parallel feature 下 `new()` 不可达 |

## 验证方式

1. 编译检查: `cargo check --all-features`
2. Lint 检查: `cargo clippy --all-targets --all-features`
3. 测试验证: `cargo test dispatch --all-features` 和不同 feature 组合下的 `cargo test dispatch`
4. 手动验证: 使用 `-- --nocapture` 检查阈值重置测试不会互相污染。

## 注意事项

- feature 关闭时模块仍编译，并回落到 Serial 或可用路径。
- `ParallelExecStrategy` 故意不 derive `PartialEq, Eq`（遵循 `30-dispatch.md §5.3`）；W10T3 的 `auto()` 等价性测试需通过字段访问器逐一比较。
