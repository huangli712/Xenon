# 实施方案: W13T5

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W13 — FFI Helpers |
| Task | W13T5 — blas.rs: `is_blas_layout_compatible()` / `blas_info()` / `lda()` inherent methods on `TensorBase` |
| 目标文件 | `src/ffi/blas.rs` |
| 前置依赖 | W13T2，W8T4（`ndim` / `shape` / `strides`），W8T5（`is_f_contiguous` / `has_zero_stride`），W8T6（`as_ptr`），W8T7（`TensorView::from_raw_parts` ——单元测试构造所需）|
| 设计文档 | 23-ffi §5.10（`is_blas_layout_compatible`）、§5.11（`blas_info`）、§5.12（`lda`）、§6.4（BLAS 检查流程）、26-error §5.1（`FfiErrorCategory`）|
| 预估工时 | 10 min |

## 目标

在 `src/ffi/blas.rs` 中作为 `TensorBase` 的 inherent methods 实现 3 个 BLAS 兼容性 API：

| 方法 | 签名 | 返回 |
|---|---|---|
| `is_blas_layout_compatible` | `&self -> bool` | 布局兼容性（仅检查 layout，不检查 ndim）|
| `blas_info` | `&self -> Result<BlasInfo<A>, XenonError>` | 完整 BLAS 元数据（含 ndim==2 检查、零行拒绝）|
| `lda` | `&self -> Result<usize, XenonError>` | 仅 leading dimension（同等校验路径）|

**所有错误均直接构造 `XenonError::Ffi { operation, category, backend, cause }`，禁止使用任何 helper 函数。**

## 前置准备

- **W2T2 已完成**：`XenonError::Ffi`、`FfiErrorCategory::{InvalidRank, BlasIncompatibleLayout}`、`FfiBackend::Blas` 可直接构造。
- **W8T4 / W8T5 已完成**：`TensorBase::{ndim, shape, strides, is_f_contiguous, has_zero_stride, as_ptr}` 可用。
- **W13T2 已完成**：`super::types::BlasInfo<A>` 已定义（字段 `data_ptr / leading_dim / rows / cols`，无 `transposed`）。
- **W8T7 已完成**：`TensorView::from_raw_parts(ptr, storage_len, shape, strides, offset)` 可用，本 task 单元测试使用该路径手工构造测试张量，**不依赖 W22 的 `Tensor::zeros` / `Tensor::from_shape_vec`**（W22 与 W13 同处 PLAN.md 可并行组 D）。
- **跨 wave 测试推迟**：`test_lda_non_contiguous` 完整覆盖需要 `transpose()` （**W20T3**）或 `slice()`（**W21T6**）产生非 F-order 布局；两者与 W13 同处组 D 互不可见，该集成测试推迟到 **W29T18 (`tests/test_ffi.rs`)**。

## 实施步骤

### Step 1: blas.rs 头部 use

```rust
//! BLAS layout compatibility queries.
//!
//! See `docs/design/23-ffi.md` §5.10–§5.12 and §6.4 for the
//! authoritative semantics; `26-error.md` §5.1 defines the structured
//! error payloads.

use core::borrow::Cow;

use super::types::BlasInfo;
use crate::dimension::Dimension;
use crate::error::{FfiBackend, FfiErrorCategory, XenonError};
use crate::storage::Storage;
use crate::tensor::TensorBase;
```

### Step 2: `is_blas_layout_compatible` — 委托给 LayoutFlags

按 `23-ffi.md` §5.10 line 842-846 + §6.4 line 1277-1289 严格复刻 —— **只检查 layout 维度无关属性**，由调用方在 `blas_info` / `lda` 中各自处理 `ndim == 2`。

```rust
impl<S, D, A> TensorBase<S, D>
where
    S: Storage<Elem = A>,
    D: Dimension,
{
    /// Checks whether the memory layout is BLAS-compatible.
    ///
    /// # BLAS Compatibility Conditions
    ///
    /// | Condition | Description |
    /// |-----------|-------------|
    /// | Contiguity | F-contiguous (Xenon only supports F-order) |
    /// | Positive strides | All strides > 0 |
    /// | No zero strides | No broadcast dimensions |
    ///
    /// # Returns
    ///
    /// `true` if the layout matches Xenon's BLAS memory-layout contract;
    /// `false` if a copy is needed first.
    ///
    /// This method **checks layout only**. Callers must still verify
    /// `ndim() == 2` and convert `rows`, `cols`, and `lda` to the
    /// BLAS/LAPACK backend integer type expected by the target
    /// implementation, typically by calling `blas_info()` and then
    /// `BlasInfo::as_blas_int()` on the exported metadata.
    pub fn is_blas_layout_compatible(&self) -> bool {
        self.is_f_contiguous() && !self.has_zero_stride()
    }
}
```

> **不要在此方法里检查 ndim**：设计 §5.10 line 827-830 + §6.4 line 1286-1288 明确把 `ndim == 2` 列为 "Additional caller-side checks"，由 `blas_info` / `lda` 各自承担。`is_blas_layout_compatible` 必须对任意 `ndim` 返回 layout-only 结果。

### Step 3: `blas_info` — 完整 BLAS 元数据，按 §5.11 全部三道闸门

按 `23-ffi.md` §5.11 line 929-990 完整复刻：闸门 1 = ndim 检查、闸门 2 = layout 兼容检查、闸门 3 = 零行拒绝；`leading_dim` 取自 `self.strides()[1]`。

```rust
impl<S, D, A> TensorBase<S, D>
where
    S: Storage<Elem = A>,
    D: Dimension,
{
    /// Returns BLAS layout identifier and parameter information.
    ///
    /// # Returns
    ///
    /// - `Ok(BlasInfo<A>)` — compatibility conditions met; `rows` /
    ///   `cols` / `leading_dim` are returned as raw `usize` metadata.
    /// - `Err(XenonError::Ffi { .. })` — returned when the tensor is not
    ///   2D, not BLAS compatible, or has zero rows (`shape[0] == 0`).
    ///
    /// Backend integer width conversion is NOT performed here; use
    /// `BlasInfo::as_blas_int::<I>(value)` to convert any of the returned
    /// `usize` sizes to `i32` / `i64` etc.
    pub fn blas_info(&self) -> Result<BlasInfo<A>, XenonError> {
        // Gate 1: rank must be 2.
        if self.ndim() != 2 {
            return Err(XenonError::Ffi {
                operation: Cow::Borrowed("ffi::blas_info"),
                category: FfiErrorCategory::InvalidRank {
                    expected: 2,
                    actual: self.ndim(),
                },
                backend: FfiBackend::Blas,
                cause: None,
            });
        }
        // Gate 2: layout must be F-contiguous without zero strides.
        if !self.is_blas_layout_compatible() {
            return Err(XenonError::Ffi {
                operation: Cow::Borrowed("ffi::blas_info"),
                category: FfiErrorCategory::BlasIncompatibleLayout {
                    shape: self.shape().to_vec(),
                    strides: self.strides().to_vec(),
                },
                backend: FfiBackend::Blas,
                cause: None,
            });
        }
        let rows = self.shape()[0];
        let cols = self.shape()[1];
        // Gate 3: BLAS requires `lda >= max(1, rows)`. For F-order shape
        // [0, n] (zero-row matrix), `product(shape) == 0` ⇒ the layout
        // is still classified F_CONTIGUOUS by `06-layout.md` §5.11's
        // HAS_ZERO_STRIDE rule, and `is_blas_layout_compatible` returns
        // true. The naturally computed F-order `strides[1]` equals
        // `rows == 0`, which violates BLAS's `lda >= 1`. Reject zero-row
        // matrices here as a separate gate so the exported `leading_dim`
        // is always `>= max(1, rows)`.
        if rows == 0 {
            return Err(XenonError::Ffi {
                operation: Cow::Borrowed("ffi::blas_info"),
                category: FfiErrorCategory::BlasIncompatibleLayout {
                    shape: self.shape().to_vec(),
                    strides: self.strides().to_vec(),
                },
                backend: FfiBackend::Blas,
                cause: None,
            });
        }
        // Post `rows > 0` gate: F-order `strides[1] == rows >= 1`, so
        // `leading_dim` always satisfies BLAS's `lda >= max(1, rows)`.
        Ok(BlasInfo {
            data_ptr: self.as_ptr(),
            leading_dim: self.strides()[1],
            rows,
            cols,
        })
    }
}
```

### Step 4: `lda` — 仅 leading dimension，同等三道闸门

按 `23-ffi.md` §5.12 line 1026-1070 复刻。

```rust
impl<S, D, A> TensorBase<S, D>
where
    S: Storage<Elem = A>,
    D: Dimension,
{
    /// Returns the leading dimension (only meaningful for 2D arrays).
    ///
    /// For F-order matrix `A[M, N]`, `LDA = stride[1]`.
    /// For zero-column matrices (`cols == 0 && rows > 0`), returns
    /// `stride[1]` (= rows for F-order) so that `lda >= max(1, rows)`
    /// is still satisfied.
    ///
    /// # Returns
    ///
    /// - `Ok(usize)` — LDA of a BLAS-compatible 2D array.
    /// - `Err(XenonError::Ffi { .. })` — returned for non-BLAS-compatible
    ///   input, non-2D input, or zero-row matrices.
    pub fn lda(&self) -> Result<usize, XenonError> {
        if self.ndim() != 2 {
            return Err(XenonError::Ffi {
                operation: Cow::Borrowed("ffi::lda"),
                category: FfiErrorCategory::InvalidRank {
                    expected: 2,
                    actual: self.ndim(),
                },
                backend: FfiBackend::Blas,
                cause: None,
            });
        }
        if !self.is_blas_layout_compatible() {
            return Err(XenonError::Ffi {
                operation: Cow::Borrowed("ffi::lda"),
                category: FfiErrorCategory::BlasIncompatibleLayout {
                    shape: self.shape().to_vec(),
                    strides: self.strides().to_vec(),
                },
                backend: FfiBackend::Blas,
                cause: None,
            });
        }
        // Mirror `blas_info()` rows-gate.
        if self.shape()[0] == 0 {
            return Err(XenonError::Ffi {
                operation: Cow::Borrowed("ffi::lda"),
                category: FfiErrorCategory::BlasIncompatibleLayout {
                    shape: self.shape().to_vec(),
                    strides: self.strides().to_vec(),
                },
                backend: FfiBackend::Blas,
                cause: None,
            });
        }
        Ok(self.strides()[1])
    }
}
```

### Step 5: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与 Lint 检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

测试函数命名对齐 `23-ffi.md` §8.2 line 1349-1353：`test_is_blas_layout_compatible` / `test_blas_info_f_order` / `test_lda_f_order` / `test_lda_non_contiguous`，并补 `test_blas_info_invalid_rank` 与 `test_blas_info_zero_rows_rejected`（覆盖 Gate 1 与 Gate 3）。

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::dimension::{Ix1, Ix2};
    use crate::layout::Strides;
    use crate::tensor::TensorView;

    /// Helper: build a TensorView<Ix2> via W8T7 `from_raw_parts`. Reusable
    /// in-test constructor that avoids depending on W22 (`Tensor::zeros` /
    /// `Tensor::from_shape_vec`); shape + canonical F-order strides only.
    fn make_view_f64_ix2<'a>(data: &'a [f64], shape: [usize; 2]) -> TensorView<'a, f64, Ix2> {
        // Canonical F-order strides for shape [m, n] are [1, m].
        let strides = [1_usize, shape[0]];
        // SAFETY: F-order canonical layout fits within data.len() = product(shape).
        unsafe {
            TensorView::<'a, f64, Ix2>::from_raw_parts(
                data.as_ptr(),
                data.len(),
                Ix2(shape[0], shape[1]),
                Strides::from_slice(&strides).unwrap(),
                0,
            )
        }
        .expect("valid F-order layout")
    }

    fn make_view_f64_ix1<'a>(data: &'a [f64]) -> TensorView<'a, f64, Ix1> {
        // SAFETY: F-order 1-D over data.
        unsafe {
            TensorView::<'a, f64, Ix1>::from_raw_parts(
                data.as_ptr(),
                data.len(),
                Ix1(data.len()),
                Strides::from_slice(&[1_usize]).unwrap(),
                0,
            )
        }
        .expect("valid F-order 1-D")
    }

    /// §8.2 test_is_blas_layout_compatible — F-order 2D tensor passes.
    #[test]
    fn test_is_blas_layout_compatible_f_order_passes() {
        let data = vec![0.0_f64; 12]; // 3 * 4 = 12
        let t = make_view_f64_ix2(&data, [3, 4]);
        assert!(t.is_blas_layout_compatible());
    }

    /// `is_blas_layout_compatible` must check layout only — `ndim != 2`
    /// alone does NOT make it return false; ndim is a caller-side check.
    #[test]
    fn test_is_blas_layout_compatible_layout_only_no_rank_check() {
        let data = vec![0.0_f64; 5];
        let t1 = make_view_f64_ix1(&data);
        // 1-D F-contiguous tensor passes the layout-only check.
        assert!(t1.is_blas_layout_compatible());
    }

    /// §8.2 test_blas_info_f_order — successful path returns correct
    /// rows/cols/leading_dim.
    #[test]
    fn test_blas_info_f_order_returns_correct_metadata() {
        let data = vec![0.0_f64; 12];
        let t = make_view_f64_ix2(&data, [3, 4]);
        let info = t.blas_info().expect("F-order 2D is BLAS compatible");
        assert_eq!(info.rows, 3);
        assert_eq!(info.cols, 4);
        assert_eq!(info.leading_dim, 3); // F-order strides[1] == rows
        assert_eq!(info.data_ptr, t.as_ptr());
    }

    /// Gate 1: non-2D input ⇒ `InvalidRank`.
    #[test]
    fn test_blas_info_invalid_rank_for_1d() {
        let data = vec![0.0_f64; 5];
        let t = make_view_f64_ix1(&data);
        let err = t.blas_info().unwrap_err();
        match err {
            XenonError::Ffi { category, backend, .. } => {
                assert!(matches!(backend, FfiBackend::Blas));
                match category {
                    FfiErrorCategory::InvalidRank { expected, actual } => {
                        assert_eq!(expected, 2);
                        assert_eq!(actual, 1);
                    }
                    other => panic!("unexpected category: {other:?}"),
                }
            }
            other => panic!("unexpected variant: {other:?}"),
        }
    }

    /// Gate 3: zero-row matrix ⇒ `BlasIncompatibleLayout`.
    /// Empty storage + shape [0, 4] is a valid empty F-order layout that
    /// passes `validate_access_range` (len=0 early-return) but fails the
    /// rows==0 gate in `blas_info`.
    #[test]
    fn test_blas_info_zero_rows_rejected() {
        let data = Vec::<f64>::new();
        // SAFETY: shape=[0,4] product=0 → validate_access_range early-returns.
        // Strides = canonical F-order for [0,4] = [1, 0] (rows=0).
        let t = unsafe {
            TensorView::<f64, Ix2>::from_raw_parts(
                data.as_ptr(),
                0,                // storage_len
                Ix2(0, 4),        // shape
                Strides::from_slice(&[1_usize, 0]).unwrap(),  // strides
                0,                // offset
            )
        }
        .expect("empty F-order [0, 4] should pass validation");
        let err = t.blas_info().unwrap_err();
        match err {
            XenonError::Ffi { category: FfiErrorCategory::BlasIncompatibleLayout { .. }, .. } => {}
            other => panic!("expected BlasIncompatibleLayout, got {other:?}"),
        }
    }

    /// §8.2 test_lda_f_order — F-order [3, 4] returns 3.
    #[test]
    fn test_lda_f_order() {
        let data = vec![0.0_f64; 12];
        let t = make_view_f64_ix2(&data, [3, 4]);
        assert_eq!(t.lda().unwrap(), 3);
    }

    /// Gate 2 placeholder: non-F-contiguous 2D should trigger
    /// `BlasIncompatibleLayout`. Constructed manually via `from_raw_parts`
    /// with C-order strides instead of relying on `transpose()` (W20T3) or
    /// `slice()` (W21T6).
    #[test]
    fn test_lda_non_f_contiguous_returns_incompatible() {
        let data = vec![0.0_f64; 12];
        // shape [3, 4] with C-order strides [4, 1] — NOT F-order.
        let strides = [4_usize, 1_usize];
        // SAFETY: shape product 12 == data.len(); strides cover [0, 11].
        let t = unsafe {
            TensorView::<f64, Ix2>::from_raw_parts(
                data.as_ptr(),
                data.len(),
                Ix2(3, 4),
                Strides::from_slice(&strides).unwrap(),
                0,
            )
        }
        .expect("valid layout (just non-F-order)");
        // is_blas_layout_compatible returns false because is_f_contiguous() is false.
        assert!(!t.is_blas_layout_compatible());
        // lda() must return BlasIncompatibleLayout (Gate 2).
        let err = t.lda().unwrap_err();
        match err {
            XenonError::Ffi { category: FfiErrorCategory::BlasIncompatibleLayout { .. }, .. } => {}
            other => panic!("expected BlasIncompatibleLayout, got {other:?}"),
        }
    }

    /// Gate 1: non-2D input to `lda()` triggers `InvalidRank` (mirrors
    /// `test_blas_info_invalid_rank_for_1d`).
    #[test]
    fn test_lda_non_2d_returns_invalid_rank() {
        let data = vec![0.0_f64; 5];
        let t = make_view_f64_ix1(&data);
        let err = t.lda().unwrap_err();
        match err {
            XenonError::Ffi { category: FfiErrorCategory::InvalidRank { expected, actual }, .. } => {
                assert_eq!(expected, 2);
                assert_eq!(actual, 1);
            }
            other => panic!("expected InvalidRank, got {other:?}"),
        }
    }
}
```

> `test_lda_non_contiguous`（设计 §8.2 line 1353）原务是测试「之前 F-contiguous、但经 transpose / slice 后变成非 F-contiguous 的虚张量」。`transpose()` 在 **W20T3**，`slice()` 在 **W21T6**，二者与 W13 同处 PLAN.md 可并行组 D，互不可见。本 task 以 `test_lda_non_f_contiguous_returns_incompatible`（手工以 C-order strides 构造 view）覆盖 Gate 2 路径，以 `test_lda_non_2d_returns_invalid_rank` 覆盖 Gate 1；transpose/slice 端到端路径的集成测试推迟到 **W29T18 (`tests/test_ffi.rs`)**，与 W19/W29T11 模式（PLAN.md §W19 推迟说明）保持一致。历史注释中「依赖 slicing API（W17 / W18）」为误记：W17 为 Matrix Ops (dot)、W18 为 Reduction Ops (sum)，不提供 slicing API。

## 关键设计决策

| 决策 | 依据 |
|------|------|
| 三个 API 全部实现为 `TensorBase` 的 inherent methods | `23-ffi.md` §5.10/§5.11/§5.12 一致写为 `impl TensorBase { pub fn ... }`；调用方使用 `tensor.blas_info()?` 等 method 形式 |
| `is_blas_layout_compatible` 不检查 `ndim()` | `23-ffi.md` §5.10 line 827-830 + §6.4 line 1286-1288 明确把 ndim 列为 "Additional caller-side checks"；layout-only 判定让 1-D 张量也可用此方法做布局诊断 |
| `is_blas_layout_compatible` 委托给 `is_f_contiguous() && !has_zero_stride()` | `23-ffi.md` §5.10 line 842-846 字面实现；不重新手写 stride 检查 —— 唯一真理源在 `06-layout.md` 的 LayoutFlags |
| `blas_info` 三道闸门顺序固定（rank → layout → rows>0） | `23-ffi.md` §5.11 line 929-977：F-order 零行矩阵会通过 layout 检查但违反 BLAS `lda >= 1`，必须独立闸门；闸门顺序保证错误诊断字段语义稳定 |
| `leading_dim` 取自 `self.strides()[1]` 而非 `rows.max(1)` | `23-ffi.md` §5.11 line 980-982：在闸门 3 之后 `strides[1] == rows >= 1`，且支持 F-order padding 布局（`strides[1] > rows` 合法）；`rows.max(1)` 会丢失 padding 信息 |
| 所有错误用 `XenonError::Ffi { operation, category, backend, cause }` 字面量直接构造 | `23-ffi.md` §5.11 line 931-940 / §5.12 line 1028-1047 给出的全部构造形式；`crate::error` 没有 `ffi_layout_error` 等 helper，禁止虚构 |
| `BlasIncompatibleLayout` payload 用 `shape.to_vec()` / `strides.to_vec()` | `26-error.md` §5.1 line 228-231 字段定义为 `Vec<usize>`；调用 `shape()`/`strides()` 返回 `&[usize]`，需通过 `.to_vec()` 上拷贝 |
| 单元测试使用 W8T7 `TensorView::from_raw_parts(ptr, storage_len, shape, strides, offset)` 构造载体，不依赖 W22 `Tensor::zeros` / `from_shape_vec` | W13 与 W22 同处 PLAN.md 可并行组 D，互不可见；对齐 PLAN.md §W19 推迟说明 的 W29T11 推迟模式，需 transpose/slice 语义的集成测试推迟到 W29T18 |
| Gate 2 (非 F-contiguous) 测试手工构造 view，不调用 `transpose()` / `slice()` | `transpose()` 在 W20T3、`slice()` 在 W21T6，与 W13 同组 D 互不可见；手工以 C-order strides `[4, 1]` 构造 [3, 4] view 能涵盖 Gate 2 路径。原“依赖 W17/W18”描述为误记，已更正为 W20/W21 + W29T18 推迟 |

## 验证方式

1. 编译检查: `cargo check`
2. Lint 检查: `cargo clippy --all-targets`
3. 测试验证: `cargo test ffi::blas`
4. 手动验证: 构造一个 F-order `Tensor<f64, Ix2>` with shape `[3, 4]`，验证：
   - `is_blas_layout_compatible() == true`
   - `blas_info().unwrap().rows == 3, cols == 4, leading_dim == 3`
   - `lda().unwrap() == 3`
   构造一个 `Tensor<f64, Ix1>`，验证：
   - `blas_info()` 返回 `Err(... InvalidRank { expected: 2, actual: 1 })`
   - `lda()` 返回相同错误

## 注意事项

- `BlasInfo` 字段填充顺序必须匹配 `23-ffi.md` §5.11 line 859-868：`data_ptr / leading_dim / rows / cols`，无 `transposed` 字段。
- `is_blas_layout_compatible` 是 layout-only 判定；调用方在使用 `blas_info().leading_dim` 之前自己检查 ndim 是不充分的 —— 应通过 `blas_info()` / `lda()` 让 Gate 1 / Gate 3 一起生效。
- `lda()` 与 `blas_info().leading_dim` 在成功路径返回相同值；分开提供是为了让仅需 LDA 的调用方避开 BlasInfo 构造开销。
- 测试构造空张量时使用 W8T7 `TensorView::from_raw_parts(ptr, 0, Ix2(0, 4), Strides::from_slice(&[1, 0]).unwrap(), 0)` 手工构造，**不**要使用 `Tensor::zeros([0, 4])` 或 `Tensor::from_shape_vec`（二者为 W22 API，与 W13 同组 D 互不可见）。
- 测试跨 wave 推迟表：需 `Tensor::zeros` / `from_shape_vec` / `transpose()` / `slice()` 语义的集成测试都迁移到 **W29T18 (`tests/test_ffi.rs`)**。`from_raw_parts` 调用参数顺序是 `(ptr, storage_len, shape, strides, offset)`，`strides` 参数是 `Strides<D>`（需 `Strides::from_slice(&[...]).unwrap()` 构造，不可传裸数组）。
Receiver 必须是 `TensorView`，不是 `Tensor`。
