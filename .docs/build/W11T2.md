# 实施方案: W11T2

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W11 — Broadcasting |
| Task | W11T2 — Module skeleton: file placeholder, rule function stubs |
| 目标文件 | `src/broadcast/shape.rs` |
| 前置依赖 | W11T1（模块入口与 `pub use`）、W2T1（`XenonError`/`BroadcastError`/`InvalidArgument`/`InvalidArgumentKind`）、W3T10（`IxDyn` 与 `IxDyn::from_vec`） |
| 设计文档 | 15-broadcast.md §5.1、§5.2、§10；26-error.md §5.1 line 124-130（BroadcastError 完整字段） |
| 预估工时 | 10 min |

## 目标

在 `src/broadcast/shape.rs` 中放置 shape 模块骨架：
1. 按 `15-broadcast.md §5.1` 声明三个公开自由函数 `can_broadcast` / `broadcast_shape` / `broadcast_strides` 的完整签名（body 全部为 `todo!()`）；
2. 引入私有 helper `broadcast_error` —— 严格对齐 `26-error.md §5.1` line 124-130 的 `BroadcastError` 字段（`operation`/`lhs_shape`/`rhs_shape`/`attempted_target_shape`/`axis`），为 W11T5/W11T6 提供结构化错误构造入口。

## 前置准备

- W11T1 已完成：`src/broadcast/mod.rs` 已 `pub use shape::{...}`，并已在 `src/lib.rs` 声明 `pub mod broadcast;`。
- W2T1 已完成：`XenonError`（含 `BroadcastError` 与 `InvalidArgument` 变体）、`InvalidArgumentKind`、`Cow<'static, str>` 错误字段可用（参见 `26-error.md §5.1`）。
- W3T10 已完成：`IxDyn` + `IxDyn::from_vec(Vec<usize>) -> Self` 关联函数可用（参见 `02-dimension.md §5.5` line 419-426）。

## 实施步骤

### Step 1: 定义 shape API 骨架（签名严格对齐设计 §5.1）

```rust
use crate::dimension::IxDyn;
use crate::error::XenonError;

/// See 15-broadcast.md §5.1 / §5.2: implemented in W11T4 (rewritten in W11T5 to
/// reuse `broadcast_shape`).
pub fn can_broadcast(shape_a: &[usize], shape_b: &[usize]) -> bool {
    let _ = (shape_a, shape_b);
    todo!("implemented by W11T4 (refined by W11T5)")
}

/// See 15-broadcast.md §5.1 / §5.2 / §6.2: returns the broadcast result shape
/// or `XenonError::BroadcastError` on incompatibility.
pub fn broadcast_shape(shape_a: &[usize], shape_b: &[usize]) -> Result<IxDyn, XenonError> {
    let _ = (shape_a, shape_b);
    todo!("implemented by W11T5")
}

/// See 15-broadcast.md §5.1 / §5.2 / §6.3: produces strides for the broadcast
/// view; zero-stride is written on broadcast axes. Returns `BroadcastError` on
/// axis incompatibility and `InvalidArgument` on input precondition failures.
pub fn broadcast_strides(
    orig_shape: &[usize],
    orig_strides: &[usize],
    target_shape: &[usize],
) -> Result<Vec<usize>, XenonError> {
    let _ = (orig_shape, orig_strides, target_shape);
    todo!("implemented by W11T6")
}
```

> **重要**：`can_broadcast` 也写成 `todo!()` 而非"复用 `broadcast_shape`"。理由：在 W11T4 完成前，`broadcast_shape` 仍是 `todo!()`，若把 `can_broadcast` 写成 `broadcast_shape(...).is_ok()`，下游早期调用 `can_broadcast` 会 panic 而非编译错误，会被误判为"已实现"。W11T4 用独立尾轴对齐逻辑实现 `can_broadcast`；W11T5 完成 `broadcast_shape` 后才允许把 `can_broadcast` 改写为 `broadcast_shape(...).is_ok()`。

### Step 2: 私有 helper —— 结构化 BroadcastError 构造

helper 严格对齐 `26-error.md §5.1` line 124-130 的 `BroadcastError` 五个字段；为 W11T5（双输入 shape 推导）与 W11T6（单输入 broadcast_strides）共用：

```rust
use std::borrow::Cow;

/// Constructs `XenonError::BroadcastError` with all fields populated per
/// `26-error.md §5.1` line 124-130.
///
/// - `operation`: caller name (e.g. "broadcast_shape", "broadcast_strides").
/// - `lhs_shape` / `rhs_shape`: the two compared shapes. For `broadcast_strides`
///   (single-input broadcast), pass `orig_shape` and `target_shape` respectively.
/// - `attempted_target_shape`: caller-supplied target if known (only relevant in
///   `broadcast_to` / `broadcast_strides` paths); `None` for pure `broadcast_shape`.
/// - `axis`: the conflicting axis index (right-aligned, 0-based on the result rank).
pub(super) fn broadcast_error(
    operation: &'static str,
    lhs_shape: &[usize],
    rhs_shape: &[usize],
    attempted_target_shape: Option<&[usize]>,
    axis: usize,
) -> XenonError {
    XenonError::BroadcastError {
        operation: Cow::Borrowed(operation),
        lhs_shape: lhs_shape.to_vec(),
        rhs_shape: rhs_shape.to_vec(),
        attempted_target_shape: attempted_target_shape.map(|s| s.to_vec()),
        axis: Some(axis),
    }
}
```

### Step 3: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或者多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与Lint检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

> 注意: 本 task 全部 API 仍为 `todo!()`，测试仅覆盖（a）签名编译检查、（b）`broadcast_error` helper 产出的错误字段完整且与 `26-error.md §5.1` 一致。功能正确性测试随 W11T4-T6 引入。

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::error::XenonError;

    #[test]
    fn test_shape_stub_signatures_compile() {
        let _: fn(&[usize], &[usize]) -> bool = can_broadcast;
        let _: fn(&[usize], &[usize]) -> Result<IxDyn, XenonError> = broadcast_shape;
        let _: fn(&[usize], &[usize], &[usize]) -> Result<Vec<usize>, XenonError> = broadcast_strides;
    }

    #[test]
    fn test_broadcast_error_has_complete_structured_fields() {
        let err = broadcast_error("broadcast_shape", &[2, 3], &[4, 3], None, 0);
        match err {
            XenonError::BroadcastError {
                operation,
                lhs_shape,
                rhs_shape,
                attempted_target_shape,
                axis,
            } => {
                assert_eq!(operation.as_ref(), "broadcast_shape");
                assert_eq!(lhs_shape, vec![2, 3]);
                assert_eq!(rhs_shape, vec![4, 3]);
                assert_eq!(attempted_target_shape, None);
                assert_eq!(axis, Some(0));
            }
            other => panic!("expected BroadcastError, got {:?}", other),
        }
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| `BroadcastError` 字段名严格对齐 26-error §5.1 line 124-130（`operation`/`lhs_shape`/`rhs_shape`/`attempted_target_shape`/`axis`） | 字段不可自创；helper 必须包含全部 5 个字段，且 `axis` 为 `Option<usize>` |
| `can_broadcast` 在 W11T2 阶段写为 `todo!()` 而非委托 `broadcast_shape` | 避免在 W11T4 完成前下游误调用 panic；W11T5 完成 `broadcast_shape` 后由 W11T5 重写为委托形式 |
| helper 可见性为 `pub(super)` | 仅 `view::broadcast_to` 与 `shape::*` 内部使用，不暴露到 crate 外 |

## 验证方式

1. 编译检查: `cargo check`
2. Lint 检查: `cargo clippy --all-targets`
3. 测试验证: `cargo test --lib broadcast::shape::tests`
4. 手动验证: 在临时调用方写 `let err = broadcast_error("x", &[1], &[2], Some(&[3]), 0);` 并用 `Debug` 打印，确认 5 个字段均存在且类型与 26-error §5.1 一致。

## 注意事项

- 严禁出现 `XenonError::BroadcastError { axis, left, right }` 或类似自创字段写法 —— `BroadcastError` 字段已被 26-error §5.1 line 124-130 封闭锁定。
- `broadcast_error` 必须接收 `operation: &'static str` 与 `attempted_target_shape: Option<&[usize]>` 两个参数，下游 W11T5/W11T6 才能各自填入正确的语义。
- `can_broadcast` 的初始 `todo!()` 版本在 W11T4 中被独立实现（不依赖 `broadcast_shape`）；W11T5 完成后再改写为 `broadcast_shape(...).is_ok()`。
