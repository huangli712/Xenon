# 实施方案: W11T8

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W11 — Broadcasting |
| Task | W11T8 — broadcast_to() error path + BroadcastView layout state update |
| 目标文件 | `src/broadcast/view.rs` |
| 前置依赖 | W11T7（`broadcast_to` 成功路径骨架，body 由本 task 改写）、W6T11（`compute_layout_flags`）、W7T14（`ViewRepr::from_raw_parts`）、W8（`TensorBase::new_unchecked` / `as_storage_ptr` / `storage_len` / `offset` / `derived_from_view_mut`） |
| 设计文档 | 15-broadcast.md §6.1 line 213、§6.4 line 258、§9.1 line 431；06-layout.md §5.5 line 233（"唯一权威计算入口"）、§5.11 line 247-269（`BroadcastView` 分类）、§5.12 line 633-651（`compute_layout_flags` 签名 + 调用方清单第 1 行明示 broadcast）、§6.1 line 680-702（实现伪码）；07-tensor.md §5 line 312（`storage_kind`）、§5.6 line 716（`new_unchecked`）、§5.3 规则 (3)（`access_semantics`）、line 465-476（`as_storage_ptr`）、line 1198（`as_ptr()` = 逻辑首指针） |
| 预估工时 | 15 min（原 10 min；路径 Y 需显式装配 ViewRepr + flags + derived_from_view_mut，较原路径 A 略重） |

## 目标

完成 `broadcast_to` 的两件事：

1. **错误路径已通过 `?` 自动传播**：W11T7 中 `broadcast_strides(...)?` 已把 `BroadcastError` / `InvalidArgument` / `InvalidLayout` 全部结构化传出；本 task **不再做预校验**（避免双重检查与语义偏离）。
2. **改写 W11T7 的视图构造调用，按路径 Y 装配 `BroadcastView`**：在 broadcast 层显式调用 `layout::compute_layout_flags` 计算结果视图的 `LayoutFlags`（含 `HAS_ZERO_STRIDE` 与 `LayoutState::BroadcastView` 分类），并通过 `TensorBase::<ViewRepr<'_, A>, E::Dim>::new_unchecked` 装配最终 `TensorView`。

> **路径决策（路径 Y，非 W11T7 沿用的路径 A）**：W11T7 当前 body 末尾使用
> `TensorView::from_raw_parts(self.as_ptr(), storage_len, target_dim, strides, self.offset())`
> 构造结果视图，存在两处违反设计契约：
> - `from_raw_parts` 一律要求 `ptr = storage base pointer`（07-tensor.md §5.1 line 202、
>   §5.7 line 752），但 `as_ptr()` 返回的是逻辑首指针（line 1198），随后再加 `offset` 会
>   双倍偏移并触发 UB。
> - `from_raw_parts` 一律将 `derived_from_view_mut` 置 `false`（07-tensor.md §5 line
>   183-186），无法传播 ViewMut 源的"降级共享只读"标记（07-tensor.md §5.3 规则 (3)）。
>
> 路径 Y 通过 `TensorBase::new_unchecked`（crate-internal canonical entry，07-tensor.md
> §5.6 line 716）显式注入 `flags` 与 `derived_from_view_mut`，并使用 `as_storage_ptr()` +
> `storage_len()` 拼装 `ViewRepr`，避开上述两个问题。W20T3（transpose）采用同一路径。

## 前置准备

- W11T7 已完成：`broadcast_to` body 当前形如 `unsafe { TensorView::from_raw_parts(self.as_ptr(), self.storage_len(), target_dim, strides, self.offset()) }`。本 task 改写该 body 末尾的视图构造为路径 Y。
- W6T11 已完成：`pub(crate) fn compute_layout_flags<A, D: Dimension>(shape: &D, strides: &Strides<D>, ptr: *const A) -> LayoutFlags`（06-layout §5.12 line 633-637）。**注意签名要求 `&D` 与 `&Strides<D>`，不接受 `&[usize]` 与 `&Vec<usize>`。**
- W6T11 内部已按 §6.1 line 684 自动处理 `HAS_ZERO_STRIDE := any(stride == 0) && product(shape) > 0`，包括 §5.11 空数组退化规则；W11 **不要**重复实现该判定逻辑。
- W7T14 已完成：`pub unsafe fn ViewRepr::from_raw_parts(ptr: *const A, len: usize) -> Self`（W7T14.md line 48）。
- W8 已完成：`TensorBase::layout_state() -> LayoutState`（07-tensor §5 line 321）；`pub(crate) unsafe fn TensorBase::new_unchecked(storage, shape, strides, offset, flags, derived_from_view_mut)`（07-tensor §5.6 line 716）；`as_storage_ptr` / `storage_len` / `offset` / `derived_from_view_mut()` accessor。`LayoutState::BroadcastView` 由 06-layout §5.11 定义。

## 实施步骤

### Step 1: 显式调用 `compute_layout_flags`（路径 Y 第一步）

按 06-layout §5.5 line 233（"LayoutFlags 的唯一权威计算入口为 `compute_layout_flags`，其他模块不得自行复算"）以及 §5.12 line 644-651 调用方清单第 1 行（"15-broadcast.md 广播视图构建后 → 须置 `HAS_ZERO_STRIDE`，重新分类为 `LayoutState::BroadcastView`"）：

```rust
use crate::layout::{compute_layout_flags, Strides};
use crate::storage::ViewRepr;
use crate::tensor::{TensorBase, TensorView};

// --- inside the `impl<S, A, D> TensorBase<S, D> { fn broadcast_to ... }` body ---

let target_dim: E::Dim = shape.into_dimension();
let target_shape: &[usize] = target_dim.slice();

// Error path: `broadcast_strides` returns BroadcastError or InvalidArgument on
// shape-level failure. No additional `broadcast_shape` pre-check is performed —
// it would be redundant (W11T6 already iterates per-axis) and semantically wrong
// (`broadcast_shape` is bidirectional; `broadcast_to` is single-direction).
let strides_vec: Vec<usize> =
    broadcast_strides(self.shape(), self.strides(), target_shape)?;
let strides: Strides<E::Dim> = Strides::<E::Dim>::from_slice(&strides_vec)?;

// Per 06-layout §5.5 line 233: the SINGLE source of truth for layout flags is
// `compute_layout_flags`. We do NOT construct `LayoutFlags::broadcast_view()` by
// hand (no such constructor exists in 06-layout.md), and we do NOT branch on
// `has_zero_stride` ourselves (§6.1 line 684 already handles
// `HAS_ZERO_STRIDE := any(stride == 0) && product(shape) > 0`, including
// empty-array degeneracy).
//
// `ptr` argument: the logical-first pointer of the RESULT view. broadcast_to
// preserves `offset`, so the result's logical-first pointer equals the source's
// logical-first pointer = `self.as_ptr()` (07-tensor.md line 1198: `as_ptr()`
// returns the logical-first pointer, NOT storage base).
let flags = compute_layout_flags::<A, E::Dim>(&target_dim, &strides, self.as_ptr());
```

### Step 2: 装配 `ViewRepr` 与 `TensorView`（路径 Y 第二步）

```rust
// (1) Build the `ViewRepr<'_, A>` that borrows the source storage.
//     ViewRepr holds `(storage_base_ptr, storage_len)` — NOT the
//     logical-first pointer (05-storage.md §6.4). The base pointer is
//     `self.as_storage_ptr()` (07-tensor.md §5 line 465-476: "returns
//     `storage.as_ptr()` directly, WITHOUT adding offset"); `storage_len()`
//     reports the source storage extent (07-tensor.md §5 line 484).
//
// SAFETY (ViewRepr::from_raw_parts, W7T14):
//   - `self.as_storage_ptr()` is non-null and aligned (carried unchanged
//     from the source storage that was already constructed and validated).
//   - `[base, base + storage_len)` lies inside a single allocation, all
//     `storage_len` elements are initialized values of `A`.
//   - The returned `ViewRepr<'_, A>` lifetime is bound to `&self`; no
//     mutable alias to the same memory is alive during that borrow.
//   - Empty-storage case: `as_storage_ptr()` returns the dangling sentinel,
//     `storage_len == 0` makes the range empty, and the sentinel is never
//     dereferenced.
let view_storage: ViewRepr<'_, A> = unsafe {
    ViewRepr::from_raw_parts(self.as_storage_ptr(), self.storage_len())
};

// (2) Finalize via `TensorBase::new_unchecked` — the canonical pub(crate)
//     unsafe constructor (07-tensor.md §5.6 line 674-730).
//
// Why NOT `TensorView::from_raw_parts` (W11T7's original path):
//   (a) Its `ptr` argument must be the storage base (07-tensor.md §5.1 line
//       202, §5.7 line 752); passing `self.as_ptr()` (logical-first) +
//       `self.offset()` would double-apply the offset → UB.
//   (b) It always sets `derived_from_view_mut := false` (07-tensor.md §5
//       line 183-186), preventing propagation from `ViewMut` sources;
//       `new_unchecked` accepts the flag explicitly.
//   (c) 07-tensor.md §5.6 line 685-690 explicitly states all internal
//       unchecked constructors forward to `new_unchecked` rather than
//       defining parallel safety invariants — broadcast is exactly such
//       an internal caller.
//
// `derived_from_view_mut` propagation per 07-tensor.md §5.6 line 707-713:
//   - `true` ONLY when source is a `ViewMutRepr` being demoted, or a
//     `ViewRepr` already carrying `derived_from_view_mut == true`.
//   - `self.derived_from_view_mut()` (07-tensor.md line 350 accessor)
//     reports the combined classification; forwarding it satisfies the
//     propagation rule for every supported source storage type.
//   - For `Owned<A>` sources the accessor returns `false`, matching the
//     §5.6 line 711-713 requirement that "Owned construction paths MUST
//     pass `false`".
//
// SAFETY (TensorBase::new_unchecked, 07-tensor.md §5.6 line 692-715):
//   - `target_dim` + `strides` + `self.offset()`: target shape is rank-checked
//     (`Strides::from_slice` succeeded; `IntoDimension` fixed the rank).
//     `broadcast_strides` already produced a layout that broadcasts the
//     source onto `target_dim`, so the result's logical access range equals
//     the source's (each `target_dim` index resolves to a source index via
//     zero-stride/repeat axes), which still fits the source `storage_len`
//     carried by `view_storage`.
//   - `target_dim.checked_size()` succeeded inside `broadcast_strides` for
//     the broadcast target; per-axis stride bounds (`<= isize::MAX`) follow
//     from inheriting source strides plus zero strides for new axes.
//   - `flags` was produced by `compute_layout_flags` (Step 1) on the same
//     `(target_dim, strides, self.as_ptr())` triple — `self.as_ptr()` is the
//     logical-first pointer the result view will expose via its own
//     `as_ptr()` (offset unchanged). This satisfies §5.6 line 693-698
//     verbatim.
//   - `derived_from_view_mut` is forwarded from the source per the
//     propagation rule above; this is an allowed source category under
//     §5.6 line 707-711.
//   - The layout family is valid for an immutable view: zero-stride
//     broadcast layouts are explicitly accepted on read-only paths
//     (07-tensor.md §5.7 line 775-777). `from_raw_parts_mut` would have
//     rejected this layout, and we are explicitly going through the
//     immutable canonical entry, so aliasing rules are not violated.
let view: TensorView<'_, A, E::Dim> = unsafe {
    TensorBase::<ViewRepr<'_, A>, E::Dim>::new_unchecked(
        view_storage,
        target_dim,
        strides,
        self.offset(),
        flags,
        self.derived_from_view_mut(),
    )
};

Ok(view)
```

> **替换说明**：本 task 删除 W11T7 末尾的 `unsafe { TensorView::from_raw_parts(...) }`
> 调用，改为本 step 描述的两段（`ViewRepr::from_raw_parts` + `TensorBase::new_unchecked`）。
> Step 1 + Step 2 一起构成 `broadcast_to` body 的最终落地路径，不再保留旧的 `from_raw_parts`
> 形式作为兜底。

### Step 3: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或者多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与Lint检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

覆盖 §8.2 中 `test_broadcast_to_error`、`test_broadcast_layout_flags_recomputed`、`test_broadcast_read_only`：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::error::XenonError;
    use crate::layout::LayoutState;
    use crate::tensor::Tensor2;

    #[test]
    fn test_broadcast_to_error() {
        let tensor: Tensor2<f64> = Tensor2::zeros([2, 3]).unwrap();
        let err = tensor.broadcast_to([4, 3]).unwrap_err();
        match err {
            XenonError::BroadcastError {
                operation,
                lhs_shape,
                rhs_shape,
                attempted_target_shape,
                axis,
            } => {
                assert_eq!(operation.as_ref(), "broadcast_strides");
                assert_eq!(lhs_shape, vec![2, 3]);
                assert_eq!(rhs_shape, vec![4, 3]);
                assert_eq!(attempted_target_shape, Some(vec![4, 3]));
                assert_eq!(axis, Some(0));
            }
            other => panic!("expected BroadcastError, got {:?}", other),
        }
    }

    /// §8.2 `test_broadcast_layout_flags_recomputed`: non-empty broadcast view
    /// (`product(shape) > 0` and `any(stride == 0)`) classifies as `BroadcastView`.
    #[test]
    fn test_broadcast_layout_flags_recomputed() {
        let tensor: Tensor2<f64> = Tensor2::zeros([1, 3]).unwrap();
        let view = tensor.broadcast_to([2, 3]).unwrap();
        assert_eq!(view.layout_state(), LayoutState::BroadcastView);
        assert!(view.flags().has_zero_stride());
    }

    /// §6.4 line 258 / §5.11 line 261-263: empty-array degenerate zero stride
    /// (`1 -> 0`) does NOT trigger `BroadcastView` classification.
    #[test]
    fn test_broadcast_to_empty_does_not_classify_as_broadcast_view() {
        let tensor: Tensor2<f64> = Tensor2::zeros([1, 3]).unwrap();
        let view = tensor.broadcast_to([0, 3]).unwrap();
        // `product(shape) == 0` ⇒ HAS_ZERO_STRIDE = false ⇒ not BroadcastView.
        assert_ne!(view.layout_state(), LayoutState::BroadcastView);
        assert!(!view.flags().has_zero_stride());
    }

    /// §8.2 `test_broadcast_read_only`: the broadcast view never exposes &mut.
    /// We verify the view is iterable in read-only fashion; the absence of any
    /// `&mut` path is covered as a compile-fail doctest in W11T10.
    #[test]
    fn test_broadcast_read_only_iterable() {
        let tensor: Tensor2<f64> =
            Tensor2::from_shape_vec([1, 3], vec![1.0, 2.0, 3.0]).unwrap();
        let view = tensor.broadcast_to([2, 3]).unwrap();
        let n: usize = view.iter().count();
        assert_eq!(n, 6);
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| 路径 Y：通过 `TensorBase::new_unchecked` 装配结果视图，不走 `TensorView::from_raw_parts` | (a) `from_raw_parts` 的 `ptr` 必须是 storage base（07-tensor.md §5.1 line 202 / §5.7 line 752），传 `as_ptr()` + `offset` 会双倍偏移 → UB；(b) `from_raw_parts` 一律把 `derived_from_view_mut` 置 false（§5 line 183-186），违反 §5.3 规则 (3) 的传播要求；(c) 07-tensor.md §5.6 line 685-690 明示"所有内部 unchecked 构造方均 forward 到 `new_unchecked`"，broadcast 即属此类 |
| 在 broadcast 层显式调用 `compute_layout_flags` | 06-layout §5.5 line 233（唯一计算入口）；§5.12 line 644-651 调用方清单第 1 行（15-broadcast.md 广播视图构建后）明确 broadcast 是 caller |
| 不在 broadcast 层手动构造 `LayoutFlags::broadcast_view()` | 06-layout §5.5 line 233 单一计算入口约束；06-layout 没有 `broadcast_view()` 构造器 |
| `ViewRepr` 基指针取 `as_storage_ptr()`，长度取 `storage_len()` | 07-tensor.md §5 line 465-476（`as_storage_ptr` 是 storage 基指针而非逻辑首指针），line 484（`storage_len()` 为 storage 长度） |
| `derived_from_view_mut` 通过 `new_unchecked` 第 6 参数显式转发 | 16-shape.md §5.1 line 159-164 / 07-tensor.md §5.3 规则 (3) 要求 ViewMut → ViewRepr 降级传播；`derived_from_view_mut()` accessor（07-tensor.md line 350）返回 Owned 恒 false、ViewMut 源恒 true，forward 值对所有 `S ∈ {Owned, ViewRepr, ViewMutRepr, ArcRepr}` 均满足 §5.6 line 707-713 |
| 不预调用 `broadcast_shape` 做双重检查 | (a) `broadcast_shape` 是双向 shape 推导，语义不匹配单向 `broadcast_to`；(b) `broadcast_strides` 已做完整 per-axis 兼容性校验 |
| 空数组退化 `1 -> 0` 不分类为 `BroadcastView` | 15-broadcast §6.1 line 213、§6.4 line 258；06-layout §5.11 line 261-263 / §6.1 line 684 (`HAS_ZERO_STRIDE := any(stride == 0) && product(shape) > 0`) |
| `compute_layout_flags` 签名严格 `(&D, &Strides<D>, *const A)` | 06-layout §5.12 line 633-637：泛型 `<A, D: Dimension>`，不接受 `&[usize]` 与 `&Vec<usize>` |

## 验证方式

1. 编译检查: `cargo check`
2. Lint 检查: `cargo clippy --all-targets`
3. 测试验证: `cargo test --lib broadcast::view::tests::test_broadcast_to_error broadcast::view::tests::test_broadcast_layout_flags_recomputed broadcast::view::tests::test_broadcast_to_empty_does_not_classify_as_broadcast_view broadcast::view::tests::test_broadcast_read_only_iterable`
4. 手动验证: REPL 中 `Tensor2::<f64>::zeros([1, 3]).unwrap().broadcast_to([2, 3]).unwrap().layout_state()` 应为 `LayoutState::BroadcastView`；`.broadcast_to([0, 3]).unwrap().layout_state()` 不应为 `BroadcastView`。

## 注意事项

- **禁止**写 `LayoutFlags::broadcast_view()` —— 06-layout.md 中**没有**这个构造器；唯一计算入口是 `compute_layout_flags(shape, strides, ptr)`。
- **禁止**调用 `compute_layout_flags::<A, E::Dim>(target_dim.slice(), &strides_vec, self.as_ptr())` —— 签名要求 `&D` 与 `&Strides<D>`，不是 `&[usize]` 与 `&Vec<usize>`。
- **禁止**写 `view.layout_state().is_broadcast_view()` —— `LayoutState` 是封闭枚举（06-layout §5.11 line 247），应使用 `view.layout_state() == LayoutState::BroadcastView` 直接比较。
- **禁止**回退到 `TensorView::from_raw_parts` 路径：该路径同时触发"`as_ptr()` + `offset` 双偏移 UB"与"`derived_from_view_mut` 被强制置 false"两个设计冲突；路径 Y 是已经过证据交叉核对的结论，W20T3（transpose）采用同一路径以保持跨任务一致性。
- **禁止**写 `Strides::<E::Dim>::try_from_slice(&strides_vec)?` —— 06-layout §5.5 line 335 的入口名是 `from_slice`；`try_from_slice` 是 `Dimension` trait 上的方法，不是 `Strides` 的。
- 不要在本 task 修改 `broadcast_with`；W11T9 单独负责。
