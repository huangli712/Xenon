# 实施方案: W9T3

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W9 — Workspace |
| Task | W9T3 — Workspace struct, constants, new(), with_default_capacity(), Drop |
| 目标文件 | `src/workspace/workspace.rs` |
| 前置依赖 | W9T1, W9T2 |
| 设计文档 | 24-workspace.md §5.1–§5.4, §8 |
| 预估工时 | 15 min |

## 目标

Workspace struct, constants, new(), with_default_capacity(), Drop。
本任务将 SUMMARY.md 中的目标落实到 `src/workspace/workspace.rs`，并与 `24-workspace §5.1–§5.4, §8` 的 API、实现拆分和测试计划保持一致。

## 前置准备

- 确认 W9T2 已创建 `src/workspace/mod.rs` 并声明 `pub mod workspace;`，确保 workspace.rs 可被模块系统发现。
- 确认 W9T1 已提供 `WorkspaceErrorCategory::{InvalidLayout, AllocFailed}` 与 `XenonError::Workspace { operation, category, cause }` 形状。
- 确认当前 crate 可使用 `alloc` 分配和 `core::sync::atomic` 原子状态。
- 按 `24-workspace.md §5.1` 保持字段拓扑（`ptr/capacity/alignment/borrow_state/split_count/_not_send_sync`）。**偏离说明**：设计文档 §5.1 (line 153-176) 展示的字段默认私有，本任务将除 `_not_send_sync` 外的 5 个字段提升为 `pub(crate)`。**理由**：W9T4/W9T5/W9T6 分别在 `borrow.rs`/`split.rs`/`expand.rs` 中通过 `impl Workspace { ... }` 直接读写 `ptr`/`capacity`/`borrow_state`/`split_count` 等字段（如 `W9T5.md:91` 的 `self.borrow_state.compare_exchange(...)`、`W9T6.md:68` 的 `self.borrow_state.load(...)`）；兄弟模块看不到默认私有字段，若不 `pub(crate)` 必须改为 getter/setter，显著增加运行时开销并破坏原子操作语义（getter 返回值无法直接参与 CAS）。`_not_send_sync` 保持私有以确保 `!Send + !Sync` 标记不被外部构造绕过。
- 按 `24-workspace.md §5.3` 保持 `new()` 行为：`capacity.max(1)` 保证非零分配；记录的 `capacity` 是请求值，不是 layout 实际值。
- 按 `24-workspace.md §5.3` (line 286-288) 保持 `with_default_capacity()` 为**零参数**函数，内部调用 `Self::new(Self::DEFAULT_CAPACITY, Self::DEFAULT_ALIGNMENT)`。

## 实施步骤

### Step 1: 实现核心代码

在 `src/workspace/workspace.rs` 中添加本任务要求的最小实现，保持职责边界与设计文档一致。

```rust
pub struct Workspace {
    pub(crate) ptr: core::ptr::NonNull<u8>,
    pub(crate) capacity: usize,
    pub(crate) alignment: usize,
    pub(crate) borrow_state: core::sync::atomic::AtomicU8,
    pub(crate) split_count: core::sync::atomic::AtomicUsize,
    _not_send_sync: core::marker::PhantomData<*mut ()>,
}

impl Workspace {
    pub const DEFAULT_ALIGNMENT: usize = 64;
    pub const MIN_ALIGNMENT: usize = 8;
    pub const DEFAULT_CAPACITY: usize = 4096;

    const BORROW_NONE: u8 = 0;
    const BORROW_READ: u8 = 1;
    const BORROW_EXCLUSIVE: u8 = 2;

    const GROWTH_FACTOR_NUMERATOR: usize = 3;
    const GROWTH_FACTOR_DENOMINATOR: usize = 2;

    pub fn capacity(&self) -> usize { self.capacity }
    pub fn alignment(&self) -> usize { self.alignment }

    pub fn new(capacity: usize, alignment: usize) -> crate::Result<Self> {
        if !alignment.is_power_of_two() || alignment < Self::MIN_ALIGNMENT {
            return Err(crate::error::XenonError::Workspace {
                operation: alloc::borrow::Cow::Borrowed("Workspace::new"),
                category: crate::error::WorkspaceErrorCategory::InvalidLayout {
                    size: capacity,
                    align: alignment,
                },
                cause: None,
            });
        }
        let size = capacity.max(1);
        let layout = alloc::alloc::Layout::from_size_align(size, alignment)
            .map_err(|_| crate::error::XenonError::Workspace {
                operation: alloc::borrow::Cow::Borrowed("Workspace::new"),
                category: crate::error::WorkspaceErrorCategory::InvalidLayout {
                    size: capacity,
                    align: alignment,
                },
                cause: None,
            })?;
        let ptr = if size == 0 {
            core::ptr::NonNull::dangling()
        } else {
            // SAFETY: layout is non-zero size and valid alignment
            let raw = unsafe { alloc::alloc::alloc(layout) };
            core::ptr::NonNull::new(raw).ok_or_else(|| {
                crate::error::XenonError::Workspace {
                    operation: alloc::borrow::Cow::Borrowed("Workspace::new"),
                    category: crate::error::WorkspaceErrorCategory::AllocFailed {
                        size,
                        align: alignment,
                    },
                    cause: None,
                }
            })?
        };
        Ok(Self {
            ptr,
            capacity,
            alignment,
            borrow_state: core::sync::atomic::AtomicU8::new(Self::BORROW_NONE),
            split_count: core::sync::atomic::AtomicUsize::new(0),
            _not_send_sync: core::marker::PhantomData,
        })
    }

    pub fn with_default_capacity() -> crate::Result<Self> {
        Self::new(Self::DEFAULT_CAPACITY, Self::DEFAULT_ALIGNMENT)
    }
}

// ---------------------------------------------------------------------------
// Drop 实现对设计文档 §5.4 的偏离说明
// ---------------------------------------------------------------------------
// 设计文档 §5.4 (line 295-306) 的 Drop 无条件使用 `Layout::from_size_align_unchecked`
// 对 `self.ptr` 调用 `dealloc`。在当前 `new()` 实现下，`capacity == 0` 时
// `self.ptr = NonNull::dangling()`（line 79-81），对 dangling pointer 执行
// `dealloc` 是未定义行为。本任务的 Drop 实现增加 `capacity > 0 && ptr 非空`
// 守护，仅在真正分配过的情况下 dealloc；当 `capacity == 0` 时跳过 dealloc。
// 这是对设计文档的**安全性修正**，不改变任何可观察语义。
// ---------------------------------------------------------------------------
impl Drop for Workspace {
    fn drop(&mut self) {
        if self.capacity > 0 && !self.ptr.as_ptr().is_null() {
            let layout = alloc::alloc::Layout::from_size_align(
                self.capacity.max(1), self.alignment,
            ).unwrap();
            // SAFETY: ptr was allocated with this layout by `new()`
            unsafe { alloc::alloc::dealloc(self.ptr.as_ptr(), layout); }
        }
    }
}
```

### Step 2: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或者多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与Lint检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

测试依据: `24-workspace §8.2`

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_workspace_new_default() {
        let ws = Workspace::with_default_capacity().unwrap();
        assert_eq!(ws.capacity(), Workspace::DEFAULT_CAPACITY);
        assert_eq!(ws.alignment(), Workspace::DEFAULT_ALIGNMENT);
    }

    #[test]
    fn test_workspace_new() {
        let ws = Workspace::new(1024, 64).unwrap();
        assert_eq!(ws.capacity(), 1024);
        assert_eq!(ws.alignment(), 64);
    }

    #[test]
    fn test_workspace_constants() {
        assert_eq!(Workspace::DEFAULT_ALIGNMENT, 64);
        assert_eq!(Workspace::MIN_ALIGNMENT, 8);
        assert_eq!(Workspace::DEFAULT_CAPACITY, 4096);
    }

    #[test]
    fn test_workspace_drop_no_leak() {
        let ws = Workspace::new(1024, 64).unwrap();
        drop(ws);
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| `_not_send_sync: PhantomData<*mut ()>` | `24-workspace.md §5.1` 保持非 Send/Sync |
| `capacity.max(1)` | 防止零大小分配导致的 null 指针问题 |
| `pub(crate)` 暴露字段给子模块 | borrow/split/expand 子模块需访问内部状态 |

## 验证方式

1. 编译检查: `cargo check`
2. Lint 检查: `cargo clippy --all-targets --all-features`
3. 测试验证: `cargo test workspace::workspace --all-features`