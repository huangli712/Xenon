# 实施方案: W13T2

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W13 — FFI Helpers |
| Task | W13T2 — types.rs: TensorExportRaw / TensorExportMutRaw / BlasInfo + ElementType / FfiErrorCategory re-exports |
| 目标文件 | `src/ffi/types.rs` 和 `src/ffi/mod.rs`（追加 `pub use`）|
| 前置依赖 | W13T1，W2T2 (`FfiErrorCategory` / `FfiBackend`)，W4T1 (`ElementType`)|
| 设计文档 | 23-ffi §5.1（FFI 错误约定）、§5.3 / §5.3.1（C-visible raw 描述符）、§5.11（`BlasInfo` 与 `as_blas_int`）|
| 预估工时 | 10 min |

## 目标

在 `src/ffi/types.rs` 中：
1. `pub use` re-export `ElementType`（来自 `crate::element`）与 `FfiErrorCategory`（来自 `crate::error`）。
2. 定义 `#[repr(C)]` 的 C-visible 非泛型描述符 `TensorExportRaw` / `TensorExportMutRaw`（`23-ffi.md` §5.3.1）。
3. 定义 `BlasInfo<A>` Rust 侧辅助类型与关联函数 `as_blas_int::<I>(value)`（`23-ffi.md` §5.11）。
4. 在 `src/ffi/mod.rs` 末尾追加对应 `pub use` 行，使 `crate::ffi::{BlasInfo, ElementType, FfiErrorCategory, TensorExportRaw, TensorExportMutRaw}` 可达。

## 前置准备

- **W2T2 已完成**：`XenonError::Ffi { operation, category, backend, cause }`、`FfiErrorCategory::IntegerOverflow { value, target_width_bits }`、`FfiBackend::Blas` 可直接构造。
- **W4T1 已完成**：`crate::element::ElementType` 枚举存在且 `#[repr(u8)]`。
- **W13T1 已完成**：`src/ffi/mod.rs` 已声明 `mod types;`，`types.rs` 占位文件存在。

## 实施步骤

### Step 1: types.rs 头部 use 与重导出

```rust
//! C-visible FFI descriptors and BLAS metadata helpers.
//!
//! See `docs/design/23-ffi.md` §5.3, §5.3.1, §5.11 for the authoritative
//! definitions and ABI rationale.

use core::ffi::c_void;
use core::borrow::Cow;

pub use crate::element::ElementType;
pub use crate::error::FfiErrorCategory;

use crate::error::{FfiBackend, XenonError};
```

### Step 2: 定义 C-visible 非泛型 raw 描述符

按 `23-ffi.md` §5.3.1 line 285-314 完整复刻字段顺序与类型。`TensorExportRaw` 与 `TensorExportMutRaw` 的唯一区别在 `data` 的 const/mut。

```rust
/// C-visible read-only tensor descriptor.
///
/// This is the cbindgen-emitted concrete schema. Generic
/// `TensorExport<'a, A>` (defined in `super::private`) is converted to
/// `TensorExportRaw` at the FFI boundary by stripping the lifetime /
/// `PhantomData` and erasing `*const A` to `*const c_void`. C consumers
/// cast `data` to the matching pointer type using `element_type` as the
/// discriminator.
#[repr(C)]
#[derive(Debug, Clone, Copy)]
pub struct TensorExportRaw {
    pub data: *const c_void,
    pub element_type: ElementType,
    pub ndim: usize,
    pub shape: *const usize,
    pub strides: *const usize,
    pub storage_len: usize,
    pub offset: usize,
}

/// C-visible mutable tensor descriptor (writable variant).
#[repr(C)]
#[derive(Debug)]
pub struct TensorExportMutRaw {
    pub data: *mut c_void,
    pub element_type: ElementType,
    pub ndim: usize,
    pub shape: *const usize,
    pub strides: *const usize,
    pub storage_len: usize,
    pub offset: usize,
}
```

> **不要 derive `Clone`/`Copy` for `TensorExportMutRaw`**：与设计 §5.3 line 305-314 + §5.3.2 一致，可写描述符不应被随意复制，避免别名指针。

### Step 3: 定义 `BlasInfo<A>` 与 `as_blas_int` 关联函数

按 `23-ffi.md` §5.11 line 859-891 复刻字段（**4 个字段，无 `transposed`**）：

```rust
/// BLAS/LAPACK matrix metadata.
///
/// BLAS/LAPACK backends may use different integer widths. Xenon keeps the
/// raw dimensions in `usize` and lets callers convert them to the backend's
/// integer type (`i32` or `i64`) at the FFI boundary via `as_blas_int()`.
///
/// Field order matches `23-ffi.md` §5.11 line 859-868 exactly.
#[derive(Debug, Clone, Copy)]
pub struct BlasInfo<A> {
    /// Data pointer to the logical first element.
    pub data_ptr: *const A,
    /// Leading dimension (element units, raw `usize`).
    pub leading_dim: usize,
    /// Number of rows.
    pub rows: usize,
    /// Number of columns.
    pub cols: usize,
}

impl<A> BlasInfo<A> {
    /// Convert a raw BLAS/LAPACK size parameter to the backend integer type.
    ///
    /// This is an associated function (no `self`): callers pick which value
    /// to convert (`info.rows`, `info.cols`, `info.leading_dim`, or any
    /// `usize` known to be a BLAS size) and select the backend integer type
    /// via turbofish, e.g. `BlasInfo::<f64>::as_blas_int::<i32>(info.rows)?`.
    ///
    /// `target_width_bits` is filled with the bit width of `I` so that the
    /// structured `FfiErrorCategory::IntegerOverflow` payload accurately
    /// identifies which backend integer type was unable to represent `value`.
    pub fn as_blas_int<I>(value: usize) -> Result<I, XenonError>
    where
        I: TryFrom<usize>,
    {
        I::try_from(value).map_err(|_| XenonError::Ffi {
            operation: Cow::Borrowed("ffi::blas_info::as_blas_int"),
            category: FfiErrorCategory::IntegerOverflow {
                value,
                target_width_bits: (core::mem::size_of::<I>() * 8) as u8,
            },
            backend: FfiBackend::Blas,
            cause: None,
        })
    }
}
```

### Step 4: 在 mod.rs 末尾追加 re-exports

按 W13T1 已声明的「模块演进协议」，本 task 在 `src/ffi/mod.rs` 末尾追加：

```rust
// src/ffi/mod.rs (append at end)
pub use types::{BlasInfo, ElementType, FfiErrorCategory, TensorExportMutRaw, TensorExportRaw};
```

### Step 5: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与 Lint 检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

测试函数命名对齐 `23-ffi.md` §8.2 单元测试清单 line 1350-1351。

```rust
#[cfg(test)]
mod tests {
    use super::*;

    /// Mirrors design §8.2: test_blas_info_f_order — success path of
    /// `as_blas_int` returning the backend integer for valid BLAS sizes.
    #[test]
    fn test_blas_info_as_blas_int_success() {
        let r: i32 = BlasInfo::<f64>::as_blas_int(3).unwrap();
        let c: i32 = BlasInfo::<f64>::as_blas_int(4).unwrap();
        let l: i32 = BlasInfo::<f64>::as_blas_int(3).unwrap();
        assert_eq!((r, c, l), (3, 4, 3));
    }

    /// Mirrors design §8.2: test_blas_info_as_blas_int_overflow.
    /// `usize::MAX` cannot fit in `i32`; conversion must return
    /// `FfiErrorCategory::IntegerOverflow` with the right `target_width_bits`.
    #[test]
    fn test_blas_info_as_blas_int_overflow() {
        let err = BlasInfo::<f64>::as_blas_int::<i32>(usize::MAX).unwrap_err();
        match err {
            XenonError::Ffi { category, backend, .. } => {
                assert!(matches!(backend, FfiBackend::Blas));
                match category {
                    FfiErrorCategory::IntegerOverflow { value, target_width_bits } => {
                        assert_eq!(value, usize::MAX);
                        assert_eq!(target_width_bits, 32);
                    }
                    other => panic!("unexpected category: {other:?}"),
                }
            }
            other => panic!("unexpected variant: {other:?}"),
        }
    }

    /// Validate the C ABI layout: raw descriptors are `#[repr(C)]` with the
    /// field order defined by §5.3.1. We verify field offsets match the
    /// design spec (§5.4 line 508-521 uses offset_of! for this purpose).
    /// MSRV 1.85 supports offset_of! (stabilized in Rust 1.77).
    #[test]
    fn test_raw_descriptors_repr_c_layout() {
        use core::mem::{align_of, offset_of, size_of};
        
        // Size lower bound check
        assert!(size_of::<TensorExportRaw>() >= size_of::<*const c_void>() + size_of::<ElementType>() + 5 * size_of::<usize>());
        
        // Alignment checks
        assert_eq!(align_of::<TensorExportRaw>(), align_of::<*const c_void>());
        assert_eq!(align_of::<TensorExportMutRaw>(), align_of::<*mut c_void>());
        
        // Field offset checks (§5.3.1 line 285-314 field order)
        assert_eq!(offset_of!(TensorExportRaw, data), 0);
        assert_eq!(offset_of!(TensorExportRaw, element_type), size_of::<*const c_void>());
        // ndim follows element_type; exact offset depends on ElementType size + padding
        assert!(offset_of!(TensorExportRaw, ndim) >= offset_of!(TensorExportRaw, element_type) + size_of::<ElementType>());
        // Remaining fields follow in order: shape, strides, storage_len, offset
        assert!(offset_of!(TensorExportRaw, shape) > offset_of!(TensorExportRaw, ndim));
        assert!(offset_of!(TensorExportRaw, strides) > offset_of!(TensorExportRaw, shape));
        assert!(offset_of!(TensorExportRaw, storage_len) > offset_of!(TensorExportRaw, strides));
        assert!(offset_of!(TensorExportRaw, offset) > offset_of!(TensorExportRaw, storage_len));
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| `TensorExportRaw` / `TensorExportMutRaw` 使用 `#[repr(C)]` + `*const c_void` / `*mut c_void` | `23-ffi.md` §5.3.1 line 285-314 要求 cbindgen 可直接导出非泛型类型；`ElementType` discriminator 替代泛型类型参数以满足 C ABI |
| `BlasInfo<A>` 不使用 `#[repr(C)]`，保留类型参数 `A` 与 `data_ptr: *const A` | `23-ffi.md` §5.11 line 859-868 定义为 Rust 侧辅助类型，非 cbindgen 导出对象；保留 `*const A` 提供类型安全的指针算术 |
| `as_blas_int` 为关联函数 `fn as_blas_int<I>(value: usize)` 而非 self method | `23-ffi.md` §5.11 line 877-890：调用方按需选 `info.rows` / `info.cols` / `info.leading_dim` 中任一字段独立转换；返回 `Result<I, _>` 而非元组，避免不必要的强耦合 |
| `BlasInfo` 字段顺序 `data_ptr, leading_dim, rows, cols`（不含 `transposed`） | 严格匹配 `23-ffi.md` §5.11 line 859-868；转置由调用方通过 BLAS `trans` 参数处理，不属于 `BlasInfo` 职责 |
| FFI 错误直接构造结构体字面量，不引入 `ffi_int_error()` 等 helper | `23-ffi.md` §5.11 line 881-889、§5.13 line 1102-1141 给出的全部错误构造均为字面量；`crate::error` 也未提供此类 helper，避免在 W13 task 中虚构 W2 模块未导出的符号 |
| `ElementType` / `FfiErrorCategory` 通过 `pub use` re-export 至 `crate::ffi` | `23-ffi.md` §5.3 line 247-248：唯一真理源在 `element` / `error` 模块；FFI 模块仅提供稳定路径，避免类型重复定义 |

## 验证方式

1. 编译检查: `cargo check`
2. Lint 检查: `cargo clippy --all-targets`
3. 测试验证: `cargo test ffi::types`
4. 手动验证: `cargo doc --no-deps -p xenon --open` 查看 `crate::ffi::BlasInfo` / `TensorExportRaw` / `TensorExportMutRaw` 的文档页面字段顺序与 `23-ffi.md` §5.3.1 / §5.11 完全一致；`cargo doc` 中应该看到 `BlasInfo::as_blas_int` 作为关联函数（无 `&self`）。

## 注意事项

- `TensorExportMutRaw` 不得 derive `Clone` 或 `Copy`：可写描述符的随意复制会破坏 `*mut` 唯一性证据。
- `BlasInfo<A>` 是普通泛型 Rust 类型，不要给它加 `#[repr(C)]` —— C 头文件无法表达泛型，cbindgen 会拒绝或乱编译。
- `as_blas_int` 不解引用 `data_ptr`，对 `*const A` 是 null 还是 dangling 不敏感，因此关联函数版本天然安全。
