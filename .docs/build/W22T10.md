# 实施方案: W22T10

## 任务概述

| 字段 | 值 |
|------|-----|
| Wave | W22 — Tensor Construction |
| Task | W22T10 — OwnedRawParts struct + into_raw_parts + from_raw_parts_owned (Owned round-trip) |
| 目标文件 | `src/tensor/construct.rs`（追加 Owned round-trip 块）, `src/tensor/mod.rs`（追加 `pub use`） |
| 前置依赖 | W8T7（`new_unchecked` 本 task 直接使用）；W7（`Owned::from_raw_parts` 本 task 直接使用）；W22T5（`from_shape_vec` / `from_vec` —— 单元测试构造载体张量所需）；W8T8（`from_raw_vec_unchecked` **间接**：W22T5 内部调用）|
| 设计文档 | 07-tensor.md §5.7 (line 890-1086), §6.2, §6.3；23-ffi.md §5.8 |
| 预估工时 | 15 min |

## 目标

在 `src/tensor/construct.rs` 中追加 **Owned 裸指针分解与重建的完整 round-trip 对**，并在 `src/tensor/mod.rs` 中通过 `pub use` 把 `OwnedRawParts` 暴露至 `crate::tensor` 公开路径，供 `crate::ffi::ptr` (W13T4) 再 re-export 给 FFI 消费者使用。

1. `pub struct OwnedRawParts<A, D> { ptr, len, cap, align, shape, strides, offset }` —— Rust-only round-trip carrier（**非** C-ABI 类型）。
2. `impl<A, D> TensorBase<Owned<A>, D>` 中追加 `fn into_raw_parts(self) -> OwnedRawParts<A, D>` 与 `unsafe fn from_raw_parts_owned(raw: OwnedRawParts<A, D>) -> Result<Self, XenonError>`。
3. 在 `src/tensor/mod.rs` 末尾追加 `pub use construct::OwnedRawParts;`，使 `crate::tensor::OwnedRawParts` 路径可用。

> **设计来源**：`07-tensor.md §5.7 line 890-1087` 给出完整结构体定义、方法签名、5 道闸门校验算法（offset==0 / shape product overflow / len == expected / cap >= len / align valid / canonical F-order strides）以及 unsafe SAFETY 契约。本任务严格按设计文档复刻，不引入任何设计之外的字段或语义。
>
> **W22T10 拆分理由**：原 `W8T7.md line 511`「注意事项」明确将 OwnedRawParts 推迟到 W22，但 W22T1-T9 任务清单中无任 task 承担该实现，形成 orphan task。`W13T4` 需要 `pub use crate::tensor::{OwnedRawParts, TensorBase}` 完成 FFI re-export，**必须**在 W22 内补足该 task 才能解锁 W13T4 的实施。

## 前置准备

1. 确认 W8T7 已完成：`new_unchecked` 单一权威 unchecked 构造器（`07-tensor.md §5.6` line 669-730）、`validate_access_range` / `validate_non_overlapping_layout` 校验函数可用。
2. 确认 W8T8 已完成：`from_raw_vec_unchecked` 已就绪。**间接依赖说明**：本 task 实现代码本身 **不** 调用 `from_raw_vec_unchecked`（使用 W7 提供的 `Owned::from_raw_parts`，见下面第 6 项）；但本 task 单元测试调用 `Tensor::from_shape_vec` / `Tensor::from_vec` 构造 round-trip 载体张量，而 W22T5 的这些构造器内部调用 W8T8 的 `from_raw_vec_unchecked` —— 所以 W8T8 是通过“测试 → W22T5 → W8T8”的间接路径依赖。
3. 确认 W22T5 已完成：`Tensor::<A, D>::from_shape_vec(shape, vec)` 与 `Tensor::<A, Ix1>::from_vec(vec)` 可用。本 task 单元测试使用这两个高层构造器制造 round-trip 载体张量（事实上所有 6 个测试函数都起始于 `Tensor::from_shape_vec` 或 `Tensor::from_vec`）。**不在本依赖列表中限制 W22T1-T4 / T6-T9**：该些 task 与本 task 同处 W22，都以 W22T1（骨架）为初始前置，PLAN.md 中本 task 被归为批次6，位于 W22T5（批次2）之后。
4. 阅读 `07-tensor.md §5.7 line 890-1086`（OwnedRawParts struct + into_raw_parts + from_raw_parts_owned 完整定义）。
5. 阅读 `26-error.md §5.1` `InvalidLayoutReason` 子枚举的以下变体（W2T1 已定义）：
   - `OwnedRequiresZeroOffset`
   - `ShapeProductOverflow`
   - `LenShapeMismatch`
   - `CapacityBelowLen`
   - `AlignmentInvalid`
   - `OwnedRequiresCanonicalFOrder`
6. 确认 `Owned<A>` 提供以下 inherent methods（W7 已实现）：
   - `Owned::from_raw_parts(ptr: *mut A, len: usize, cap: usize, align: usize) -> Self`（unsafe）
   - `Owned::len(&self) -> usize`、`Owned::capacity(&self) -> usize`、`Owned::alignment(&self) -> usize`

## 实施步骤

### Step 1: construct.rs 头部追加 use

在 `src/tensor/construct.rs` 现有 `use` 列表中确认或追加以下导入（部分可能已被 W8T7/T8 引入）：

```rust
use core::borrow::Cow;
use core::mem::ManuallyDrop;
use core::ptr::NonNull;

use crate::dimension::Dimension;
use crate::error::{InvalidLayoutReason, StorageKindTag, XenonError};
use crate::layout::{self, Strides};
use crate::storage::Owned;
```

### Step 2: 定义 `OwnedRawParts<A, D>` 结构体

按 `07-tensor.md §5.7 line 903-911` 字面复刻 —— **7 个字段** 按 `ptr / len / cap / align / shape / strides / offset` 顺序，无 `_marker`、无 `PhantomData`、无 `#[repr(C)]`。

```rust
/// Decomposition of an owned tensor into raw pointer + allocator metadata.
///
/// # ABI Note
///
/// `OwnedRawParts<A, D>` is **not** a stable C-ABI type. The `D` and
/// `Strides<D>` fields are Rust generics whose layout is not specified
/// by `#[repr(C)]` (especially for `IxDyn`, which contains a `Vec<usize>`).
/// FFI consumers MUST NOT decode this struct from C code. It exists solely
/// as a Rust-internal round-trip carrier for `into_raw_parts` /
/// `from_raw_parts_owned`. C-facing interop must use the dedicated
/// `TensorExportRaw` / `TensorExportMutRaw` types defined in `23-ffi.md §5.3.1`,
/// which are explicitly designed for stable C ABI.
///
/// See `07-tensor.md §5.7` for the authoritative field semantics and the
/// Owned round-trip contract.
pub struct OwnedRawParts<A, D> {
    /// Pointer to the storage base. Ownership transferred to the consumer
    /// upon `into_raw_parts`; reclaimed by `from_raw_parts_owned`.
    pub ptr: *mut A,
    /// Logical length in elements (matches `shape.checked_size()`).
    pub len: usize,
    /// Allocator capacity in elements (`cap >= len`).
    pub cap: usize,
    /// Allocator alignment in bytes (power of two, `>= align_of::<A>()`).
    pub align: usize,
    /// Owned tensor shape; for `into_raw_parts` always satisfies
    /// `shape.checked_size().unwrap() == len`.
    pub shape: D,
    /// Canonical F-order strides matching `shape`.
    pub strides: Strides<D>,
    /// Logical offset in element units; **must be 0** for owned raw parts
    /// (enforced by `from_raw_parts_owned`).
    pub offset: usize,
}
```

> **不要 derive `Clone` / `Copy` / `Debug`**：`*mut A` 本身禁止 `Copy`；`D` 含 `IxDyn` 时不一定 `Copy`；`Debug` 在裸指针场景容易误导，且设计文档 §5.7 没有要求。

### Step 3: 实现 `into_raw_parts`

按 `07-tensor.md §5.7` line 913-948 严格复刻。**核心要点**：
- 必须 `impl<A, D> TensorBase<Owned<A>, D> where D: Dimension + Clone`（`Clone` 约束来自 §5.7 line 915 + 943-944 的注释）。
- 使用 `ManuallyDrop` 包裹 `self`，避免 `into_raw_parts` 返回时 `self` 被 Drop 释放底层内存。
- `ptr` 取自 `storage` 的 base pointer（**非** logical first，因为 owned `offset == 0` 时两者一致）。
- `len` / `cap` / `align` 从 `Owned<A>` 的 inherent methods 读取。

```rust
impl<A, D> super::TensorBase<Owned<A>, D>
where
    D: Dimension + Clone,
{
    /// Consumes the tensor, returning owned raw parts.
    ///
    /// # Returns
    ///
    /// An `OwnedRawParts<A, D>` snapshot containing the pointer plus the
    /// allocator metadata required to reconstruct Xenon's aligned owned
    /// storage.
    ///
    /// # Ownership Transfer
    ///
    /// This method consumes `self`. The caller MUST eventually reconstruct
    /// the tensor via `from_raw_parts_owned` and let `Drop` reclaim the
    /// memory, or else memory is leaked. Calling system `free` or any
    /// foreign allocator on `raw.ptr` is UB because Xenon's aligned
    /// allocator uses a specific (cap, align) pair recorded only in the
    /// returned `OwnedRawParts`.
    pub fn into_raw_parts(self) -> OwnedRawParts<A, D> {
        let this = ManuallyDrop::new(self);
        // SAFETY: `this` is a valid owned tensor; the storage base pointer
        // is exactly the pointer whose ownership is being transferred to the
        // caller as part of the returned raw parts. `as_mut_ptr_unchecked`
        // is the Owned inherent method that returns the storage base
        // mutable pointer without aliasing checks (W7 internal API).
        let ptr = this.storage.as_mut_ptr_unchecked();
        OwnedRawParts {
            ptr,
            len: this.storage.len(),
            cap: this.storage.capacity(),
            align: this.storage.alignment(),
            shape: this.shape.clone(),
            strides: this.strides.clone(),
            offset: this.offset,
        }
    }
}
```

> **关于 `storage.as_mut_ptr_unchecked()`**：`Owned<A>` 必须提供一个 `pub(crate) fn as_mut_ptr_unchecked(&self) -> *mut A` 内部方法以避免 `ManuallyDrop` 内的可变借用冲突（同时 `len`/`cap`/`align` 读取都是 `&self`）。如果 W7 中尚未提供该方法，本 task 需要先在 `src/storage/owned.rs` 补一行 inherent method（不变更 trait 边界，不破坏 W7 公开 API）。如果 W7 已经提供等价命名（如 `storage_base_mut_ptr`），按实际命名调用。

### Step 4: 实现 `from_raw_parts_owned` —— 5 道闸门校验

按 `07-tensor.md §5.7` line 950-1086 严格复刻 5 道闸门顺序：

1. **Gate 1**: `raw.offset == 0` —— 否则 `InvalidLayoutReason::OwnedRequiresZeroOffset`
2. **Gate 2**: `raw.shape.checked_size()` 与 `raw.len` 相等 —— 溢出报 `ShapeProductOverflow`，不等报 `LenShapeMismatch`
3. **Gate 3**: `raw.cap >= raw.len` —— 否则 `CapacityBelowLen`
4. **Gate 4**: `raw.align.is_power_of_two() && raw.align >= align_of::<A>()` —— 否则 `AlignmentInvalid`
5. **Gate 5**: `raw.strides == compute_f_strides(&raw.shape)?` —— 否则 `OwnedRequiresCanonicalFOrder`

所有闸门通过后，构造 `Owned` 存储，计算 `LayoutFlags`，通过 `new_unchecked` 路由到单一权威 unchecked 构造器。

```rust
impl<A, D> super::TensorBase<Owned<A>, D>
where
    A: crate::element::Element,
    D: Dimension + Clone + PartialEq,
{
    /// Reconstructs an owned tensor from raw parts obtained via
    /// `into_raw_parts`. Takes ownership of memory allocated by Xenon's
    /// aligned allocator.
    ///
    /// # Safety
    ///
    /// - `raw.ptr` must point to memory allocated by Xenon's aligned
    ///   allocator with the recorded `(cap, align)` pair.
    /// - `raw.len`, `raw.cap`, and `raw.align` must be the original allocator
    ///   metadata (as returned by `into_raw_parts`).
    /// - `raw.shape` and `raw.strides` must describe a valid, non-overlapping
    ///   canonical F-order layout (enforced by Gate 5).
    /// - `raw.offset` must be 0 (enforced by Gate 1).
    /// - The caller transfers ownership; do NOT free `raw.ptr` separately.
    ///
    /// # Errors
    ///
    /// Returns `Err(XenonError::InvalidLayout { reason, .. })` when directly
    /// checkable metadata validation fails. The five gate-specific
    /// `InvalidLayoutReason` variants are listed above; the unsafe obligation
    /// remains the memory/pointer guarantees that cannot be checked from
    /// metadata alone.
    pub unsafe fn from_raw_parts_owned(
        raw: OwnedRawParts<A, D>,
    ) -> Result<Self, XenonError> {
        // Gate 1: offset must be zero for owned raw parts.
        if raw.offset != 0 {
            return Err(XenonError::InvalidLayout {
                operation: Cow::Borrowed("tensor::from_raw_parts_owned"),
                storage_kind: StorageKindTag::Owned,
                shape: raw.shape.slice().to_vec(),
                strides: raw.strides.as_slice().to_vec(),
                offset: raw.offset,
                storage_len: raw.len,
                reason: InvalidLayoutReason::OwnedRequiresZeroOffset,
            });
        }

        // Gate 2: shape product must be representable AND equal raw.len.
        let expected_len = raw.shape.checked_size().map_err(|_| XenonError::InvalidLayout {
            operation: Cow::Borrowed("tensor::from_raw_parts_owned"),
            storage_kind: StorageKindTag::Owned,
            shape: raw.shape.slice().to_vec(),
            strides: raw.strides.as_slice().to_vec(),
            offset: raw.offset,
            storage_len: raw.len,
            reason: InvalidLayoutReason::ShapeProductOverflow,
        })?;
        if raw.len != expected_len {
            return Err(XenonError::InvalidLayout {
                operation: Cow::Borrowed("tensor::from_raw_parts_owned"),
                storage_kind: StorageKindTag::Owned,
                shape: raw.shape.slice().to_vec(),
                strides: raw.strides.as_slice().to_vec(),
                offset: raw.offset,
                storage_len: raw.len,
                reason: InvalidLayoutReason::LenShapeMismatch,
            });
        }

        // Gate 3: capacity must cover len.
        if raw.cap < raw.len {
            return Err(XenonError::InvalidLayout {
                operation: Cow::Borrowed("tensor::from_raw_parts_owned"),
                storage_kind: StorageKindTag::Owned,
                shape: raw.shape.slice().to_vec(),
                strides: raw.strides.as_slice().to_vec(),
                offset: raw.offset,
                storage_len: raw.len,
                reason: InvalidLayoutReason::CapacityBelowLen,
            });
        }

        // Gate 4: align must be a valid power of two and at least align_of::<A>().
        if !raw.align.is_power_of_two() || raw.align < core::mem::align_of::<A>() {
            return Err(XenonError::InvalidLayout {
                operation: Cow::Borrowed("tensor::from_raw_parts_owned"),
                storage_kind: StorageKindTag::Owned,
                shape: raw.shape.slice().to_vec(),
                strides: raw.strides.as_slice().to_vec(),
                offset: raw.offset,
                storage_len: raw.len,
                reason: InvalidLayoutReason::AlignmentInvalid,
            });
        }

        // Gate 5: strides must equal canonical F-order strides.
        let expected_strides = layout::compute_f_strides(&raw.shape)?;
        if raw.strides != expected_strides {
            return Err(XenonError::InvalidLayout {
                operation: Cow::Borrowed("tensor::from_raw_parts_owned"),
                storage_kind: StorageKindTag::Owned,
                shape: raw.shape.slice().to_vec(),
                strides: raw.strides.as_slice().to_vec(),
                offset: raw.offset,
                storage_len: raw.len,
                reason: InvalidLayoutReason::OwnedRequiresCanonicalFOrder,
            });
        }

        // SAFETY: Caller's # Safety contract guarantees raw.ptr is valid
        // memory allocated by Xenon's aligned allocator with the recorded
        // (len, cap, align) metadata. Ownership transfer is part of the
        // contract; raw.ptr must not be freed externally.
        let storage = unsafe { Owned::from_raw_parts(raw.ptr, raw.len, raw.cap, raw.align) };

        let logical_ptr: *const A = if raw.len == 0 {
            // Empty tensors: use a well-defined non-dereferenceable sentinel
            // rather than a potentially dangling storage pointer.
            NonNull::<A>::dangling().as_ptr()
        } else {
            // offset == 0 already verified; raw.ptr IS the logical first element.
            raw.ptr
        };
        let flags = layout::compute_layout_flags::<A, D>(&raw.shape, &raw.strides, logical_ptr);

        // SAFETY (new_unchecked, six-input invariant):
        //   (1) shape was overflow-checked above (Gate 2);
        //   (2) strides were verified canonical F-order (Gate 5);
        //   (3) offset == 0 was verified (Gate 1);
        //   (4) flags were just produced by compute_layout_flags for the
        //       same shape/strides/logical_ptr;
        //   (5) the logical access range [0, raw.len) lies within storage
        //       because raw.len == shape.checked_size() (Gate 2) and
        //       raw.cap >= raw.len (Gate 3);
        //   (6) derived_from_view_mut: false — from_raw_parts_owned is an
        //       Owned reconstruction, NOT a ViewMut downgrade.
        Ok(unsafe {
            super::TensorBase::new_unchecked(storage, raw.shape, raw.strides, raw.offset, flags, false)
        })
    }
}
```

### Step 5: 在 `src/tensor/mod.rs` 追加 `pub use`

按项目「模块声明演进协议」（W1T3 / W8T1），在 `src/tensor/mod.rs` 既有 `pub use` 之后追加：

```rust
// src/tensor/mod.rs (append after existing pub use construct::*; or pub use construct::{...};)
pub use construct::OwnedRawParts;
```

这是 `crate::ffi::ptr` (W13T4) `pub use crate::tensor::{OwnedRawParts, TensorBase};` 能够编译的前置条件。

### Step 6: 单元测试

> **强制要求**:
> - 本步骤必须包含一个或多个单元测试。
> - 测试函数名称和测试内容必须参考对应设计文档的 **「测试计划」** 一节。
> - 编译检查与 Lint 检查不等同于单元测试。
> - 如果测试没有通过需要修复直至测试通过。
> - **无测试 = 任务未完成。**

测试函数命名对齐 `07-tensor.md §8.2` 测试清单：`test_into_raw_parts_roundtrip` / `test_from_raw_parts_owned_*_gate*`。

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::dimension::{Ix1, Ix2};
    use crate::tensor::Tensor;

    /// §8.2 test_from_raw_parts_roundtrip — into_raw_parts → from_raw_parts_owned
    /// round-trip preserves shape / strides / offset / element contents.
    #[test]
    fn test_into_raw_parts_roundtrip_2d() {
        let original = Tensor::<i32, Ix2>::from_shape_vec([2, 3], vec![1, 2, 3, 4, 5, 6]).unwrap();
        let raw = original.into_raw_parts();
        assert_eq!(raw.len, 6);
        assert!(raw.cap >= 6);
        assert_eq!(raw.offset, 0);
        assert_eq!(raw.shape.slice(), &[2, 3]);
        // SAFETY: raw came directly from into_raw_parts; no external mutation.
        let restored: Tensor<i32, Ix2> = unsafe { Tensor::from_raw_parts_owned(raw) }
            .expect("round-trip must succeed");
        assert_eq!(restored.shape(), &[2, 3]);
        assert_eq!(restored.as_slice().unwrap(), &[1, 2, 3, 4, 5, 6]);
    }

    /// Round-trip for 1D tensors via the from_vec convenience path.
    #[test]
    fn test_into_raw_parts_roundtrip_1d() {
        let original = Tensor::<f64, Ix1>::from_vec(vec![1.0, 2.0, 3.0, 4.0]).unwrap();
        let raw = original.into_raw_parts();
        let restored: Tensor<f64, Ix1> = unsafe { Tensor::from_raw_parts_owned(raw) }.unwrap();
        assert_eq!(restored.as_slice().unwrap(), &[1.0, 2.0, 3.0, 4.0]);
    }

    /// Empty tensor round-trip — len=0 path must use dangling sentinel for
    /// compute_layout_flags input.
    #[test]
    fn test_into_raw_parts_roundtrip_empty() {
        let original = Tensor::<i32, Ix1>::from_shape_vec([0], Vec::new()).unwrap();
        let raw = original.into_raw_parts();
        assert_eq!(raw.len, 0);
        let restored: Tensor<i32, Ix1> = unsafe { Tensor::from_raw_parts_owned(raw) }.unwrap();
        assert_eq!(restored.len(), 0);
    }

    /// Gate 1: from_raw_parts_owned rejects non-zero offset.
    #[test]
    fn test_from_raw_parts_owned_rejects_nonzero_offset() {
        let original = Tensor::<i32, Ix1>::from_vec(vec![1_i32, 2, 3]).unwrap();
        let mut raw = original.into_raw_parts();
        raw.offset = 1; // Tamper with the offset.
        let err = unsafe { Tensor::<i32, Ix1>::from_raw_parts_owned(raw) }.unwrap_err();
        assert!(matches!(
            err,
            crate::error::XenonError::InvalidLayout {
                reason: crate::error::InvalidLayoutReason::OwnedRequiresZeroOffset,
                ..
            }
        ));
    }

    /// Gate 2 (LenShapeMismatch): tampered shape produces wrong expected length.
    #[test]
    fn test_from_raw_parts_owned_rejects_len_shape_mismatch() {
        let original = Tensor::<i32, Ix2>::from_shape_vec([2, 3], vec![1, 2, 3, 4, 5, 6]).unwrap();
        let mut raw = original.into_raw_parts();
        // Tamper: claim shape [3, 3] (expected_len=9) but len stays at 6.
        // Gate 2 (LenShapeMismatch) fires before Gate 5 because the check
        // order in `from_raw_parts_owned` is offset → shape → cap → align → strides.
        raw.shape = crate::dimension::Ix2(3, 3);
        let err = unsafe { Tensor::<i32, Ix2>::from_raw_parts_owned(raw) }.unwrap_err();
        assert!(matches!(
            err,
            crate::error::XenonError::InvalidLayout {
                reason: crate::error::InvalidLayoutReason::LenShapeMismatch,
                ..
            }
        ));
    }

    /// Gate 5 (OwnedRequiresCanonicalFOrder): tampered strides reject.
    #[test]
    fn test_from_raw_parts_owned_rejects_non_canonical_strides() {
        let original = Tensor::<i32, Ix2>::from_shape_vec([2, 3], vec![1, 2, 3, 4, 5, 6]).unwrap();
        let mut raw = original.into_raw_parts();
        // Tamper strides — replace canonical [1, 2] with C-order [3, 1].
        raw.strides = crate::layout::Strides::from_slice(&[3, 1]).unwrap();
        let err = unsafe { Tensor::<i32, Ix2>::from_raw_parts_owned(raw) }.unwrap_err();
        assert!(matches!(
            err,
            crate::error::XenonError::InvalidLayout {
                reason: crate::error::InvalidLayoutReason::OwnedRequiresCanonicalFOrder,
                ..
            }
        ));
    }
}
```

## 关键设计决策

| 决策 | 依据 |
|------|------|
| `OwnedRawParts<A, D>` 不 derive `Clone/Copy/Debug` | `*mut A` 禁止 `Copy`；`IxDyn` 含 `Vec` 不 `Copy`；`Debug` 在裸指针场景误导；`07-tensor.md §5.7` 没有要求这些 derive |
| `OwnedRawParts<A, D>` **不**加 `#[repr(C)]` | `07-tensor.md §5.7 line 895-902` 明确：这是 Rust-only round-trip carrier，**非** C-ABI 类型；C 消费者用 `TensorExportRaw` (W13T2) |
| `into_raw_parts` 用 `ManuallyDrop` 避免 Drop 释放底层内存 | `07-tensor.md §5.7 line 933` 标准 Rust 惯用法；所有权转移给调用方 |
| 5 道闸门按 `offset → shape → cap → align → strides` 顺序 | `07-tensor.md §5.7 line 974-1047` 字面顺序；闸门顺序确保错误诊断字段语义稳定（先拒最便宜的检查） |
| Gate 2 复用 `shape.checked_size()` 的错误转换为 `ShapeProductOverflow` | `07-tensor.md §5.7 line 988-996` —— `map_err` 把 `checked_size` 的失败映射到结构化 `InvalidLayoutReason` |
| Gate 5 通过 `compute_f_strides(&shape)?` + `PartialEq` 比较 | `07-tensor.md §5.7 line 1036-1037` —— canonical F-order 是 owned 不变量的一部分 |
| `from_raw_parts_owned` 最终路由到单一权威 `new_unchecked` | `07-tensor.md §5.6 line 669-690` —— 项目锁定的"6 字段 TensorBase 单一构造入口"约束 |
| 空张量用 `NonNull::<A>::dangling().as_ptr()` 作 `compute_layout_flags` 输入 | `07-tensor.md §5.7 line 1055-1058` —— 避免向 `compute_layout_flags` 喂可能未分配的 storage pointer |
| `D: Dimension + Clone + PartialEq` trait bound | `Clone` 用于 `into_raw_parts` 复制 shape/strides（line 944-945）；`PartialEq` 用于 Gate 5 比较 strides（line 1037） |
| 单元测试覆盖 round-trip 主路径 + 3 个 Gate 错误路径 | `07-tensor.md §8.2` 明确 `test_from_raw_parts_roundtrip` / `test_into_raw_parts` 为高优先级，本任务额外补 Gate 失败路径以达到结构化错误测试覆盖 |

## 验证方式

1. 编译检查: `cargo check`
2. Lint 检查: `cargo clippy --all-targets`
3. 测试验证: `cargo test tensor::construct::tests::test_into_raw_parts_roundtrip` 与 `cargo test tensor::construct::tests::test_from_raw_parts_owned_rejects`
4. 手动验证:
   - 构造 `Tensor::<f64, Ix2>::from_shape_vec([3, 4], (0..12).map(|i| i as f64).collect())`，调用 `into_raw_parts()` 验证 `raw.len == 12, raw.offset == 0, raw.shape == Ix2(3, 4)`
   - 用同一个 `raw` 调用 `unsafe { Tensor::from_raw_parts_owned(raw) }`，验证返回 `Ok` 且 `restored.as_slice().unwrap()` 与原始 vec 数据完全一致
   - 篡改 `raw.offset = 1` 调用 `from_raw_parts_owned`，必须返回 `InvalidLayout { reason: OwnedRequiresZeroOffset, .. }`
   - 检查 `crate::tensor::OwnedRawParts` 路径在 crate 外可达（W13T4 依赖该路径）

## 注意事项

- `OwnedRawParts<A, D>` **不**实现 `Send` / `Sync`（裸指针默认 `!Send + !Sync`）。如果调用方需要跨线程移交，必须自行 `unsafe impl`，并对底层分配器的线程安全负责。这与 `Tensor<A, D>` 的 `Send`/`Sync` 推导路径不同 —— 后者由 `Owned<A>` 的 `Send`/`Sync` impls 在 W7T12 中提供。
- `into_raw_parts` 消费 `self`，**不可再次访问原 tensor**。调用方必须保证 `OwnedRawParts` 最终通过 `from_raw_parts_owned` 重建并 Drop，否则**内存泄漏**。Xenon 没有 garbage collector，泄漏不会自动恢复。
- `from_raw_parts_owned` 是 `unsafe fn`，5 道闸门只能验证元数据合法性；**调用方仍负责** 指针来源、所有权未被他处持有、分配器匹配等无法从元数据推断的前提。这些前提违反时即 UB，**不**是 recoverable error。
- `crate::tensor::OwnedRawParts` 是 W13T4 `pub use crate::tensor::{OwnedRawParts, TensorBase};` 的依赖前置。本 task 完成后，W13T4 才能编译通过。
- 如果 W7 的 `Owned<A>` 缺少 `as_mut_ptr_unchecked` / `from_raw_parts` 等内部 inherent methods，本 task 实施时需先在 W7 补足（视为 W22T10 的实施前置 prerequisite，不另立 task）。
